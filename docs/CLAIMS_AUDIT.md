# Claims-versus-evidence audit (C1)

One row per declarative claim in the paper. Claims are enumerated mechanically from
`PAPER.template.md` by `scripts/task_c1_claims_audit.py`; the verdict column is the
only judgement, and is keyed to the claim's text so that rewording a claim resets it
to UNREVIEWED rather than carrying a stale verdict forward.

**311 claims.** SUPPORTED: 244, UNREVIEWED: 67

| # | § | claim | backed by | verdict |
|---|---|---|---|---|
| 1 | Abstract | The base paper's central training claim reproduces.** Under a rule committed to git before the runs that tested it, autoregressive training beats teac… | `task_d1_threeseed.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 2 | Abstract | Neither uncertainty output the follow-up adds is usable as an interval.** At h = {{v2_deploy_h}}, the horizon its own imagination rollouts run to, the… | `task_d_nind20.json`, `v2_deployment_horizon.json`, `v3_metric_definitions.json` | **UNREVIEWED** |
| 3 | Abstract | The per-member σ, which the method computes and discards, is a further {{d1n_epi_over_alea_h100}}× worse at h = {{v2_deploy_h}}, and we derive why: th… | `task_d_nind20.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 4 | Abstract | As a ranking it survives adversarial testing.** It beats the forecast step index — a free counter neither paper ran — at every horizon, and with the r… | `a2_trajectory_level_control.json` | **UNREVIEWED** |
| 5 | Abstract | The interval is repairable.** One multiplier per horizon, fitted on one held-out episode and scored on the other, restores nominal coverage on every h… | `task_d3_perhorizon.json` | **UNREVIEWED** |
| 6 | 1. Introduction | A world model that reports its own uncertainty is more useful than one that does not, and the uncertainty-aware Robotic World Model reports one. | — | **SUPPORTED** |
| 7 | 1. Introduction | But the same rebuild made a second question cheap to ask, because we had a from-scratch model, the released checkpoint, and a harness that could score… | — | **SUPPORTED** |
| 8 | 1. Introduction | We rebuilt rather than imported.** The forward pass, the loss and the training step are written from scratch and then checked against the reference: o… | `step4_3_differential.json` | **SUPPORTED** |
| 9 | 1. Introduction | We retract our own findings when they fail.** {{n_retractions_word}} numbered claims in this work are withdrawn on evidence this project produced, and… | `FINDINGS_LEDGER.md` | **SUPPORTED** |
| 10 | 1. Introduction | All of them are kept in the record rather than deleted (§9). | — | **SUPPORTED** |
| 11 | 1. Introduction | A from-scratch reimplementation verified at the gradient level.** Outputs match the released module bitwise; losses and gradients match to {{diff_grad… | `step4_3_differential.json`, `task_d1_threeseed.json` | **UNREVIEWED** |
| 12 | 1. Introduction | Both outputs are overconfident by one to four orders of magnitude, with intervals over independent trajectories at every horizon; and the aleatoric co… | `r2_independent_ensemble.json`, `v1_ensemble_topology.json` | **UNREVIEWED** |
| 13 | 1. Introduction | The decomposition says what that is made of: σ larger by {{r2_sigma_x_h100}}×, {{r2_from_sigma_h100}}% of the improvement at h = {{v2_deploy_h}}, reve… | `FINDINGS_LEDGER.md`, `r2_independent_ensemble.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 14 | 2. Related work | We ask it of a **released checkpoint** from a pipeline its authors deployed on hardware, where the interval reading is consequential; we measure **cov… | — | **SUPPORTED** |
| 15 | 2. Related work | Where the parameterisation comes from, and why it matters more than one repository.** The bounded log-σ head that §6.3 shows has its optimum at σ = 0 … | — | **SUPPORTED** |
| 16 | 2. Related work | and `architectures/mlp.py:92-93` is those two lines in log-standard-deviation rather than log-variance, with `system_dynamics.py:302` supplying PETS's… | — | **SUPPORTED** |
| 17 | 2. Related work | So the collapse §6.3 derives follows from the *substitution*, not from the parameterisation — and that makes §6.3 larger than one repository: **any de… | — | **SUPPORTED** |
| 18 | 2. Related work | That definition is the reference point for §6.4: the released checkpoint's five members share one GRU trunk, one recurrent hidden state, and {{v1_shar… | `v1_ensemble_topology.json` | **SUPPORTED** |
| 19 | 2. Related work | Why miscalibration is the expected finding rather than a surprising one.** Modern networks are systematically miscalibrated (Guo, Pleiss, Sun and Wein… | — | **SUPPORTED** |
| 20 | 2. Related work | Every entry above was checked against the paper itself — title, full author list, venue and year from the arXiv record, and for any sentence we attrib… | `t1_bibliography_verified.json` | **SUPPORTED** |
| 21 | 3. Setup | Data.** The released dataset is {{rows}} rows of ANYmal D proprioceptive state and policy actions at 50 Hz. | `step0_regimes.json` | **SUPPORTED** |
| 22 | 3. Setup | The reference window builder therefore marks all {{win_naive}} windows valid, including {{win_cross}} that splice one episode's end onto the next one'… | `step0_regimes.json` | **SUPPORTED** |
| 23 | 3. Setup | The usable, episode-respecting count is {{win_usable}} — {{rows}} rows, less {{win_tail}} that cannot start a full window, less {{win_cross}} that cro… | `step0_regimes.json` | **SUPPORTED** |
| 24 | 3. Setup | The contamination rate is {{contam_pct}}%. | `step0_regimes.json` | **SUPPORTED** |
| 25 | 3. Setup | The paper describes two loss terms; the implementation has {{diff_terms}}. | `step4_3_differential.json` | **SUPPORTED** |
| 26 | 3. Setup | Two 400-step trajectories whose spans overlap are not independent evidence, and the out-of-sample arena contains only {{m23_nind}} mutually non-overla… | `task5_analysis.json` | **SUPPORTED** |
| 27 | 3.1 Metrics | Every metric below is stated as implemented, with the `file:line` of the implementation; each citation is read back and checked against its own source… | `v3_metric_definitions.json` | **SUPPORTED** |
| 28 | 3.1 Metrics | with $t_0$ = `history_horizon` = {{v2_history}}: the first {{v2_history}} steps are teacher-forced and excluded. | `v2_deployment_horizon.json` | **SUPPORTED** |
| 29 | 3.1 Metrics | A value of 1.0 means no better than predicting the training mean. | — | **SUPPORTED** |
| 30 | 3.1 Metrics | And because the statistic is built from an *absolute* error, $z \le k$ is the two-sided event, so the calibrated targets are $\mathrm{erf}(k/\sqrt{2})… | `v3_metric_definitions.json` | **SUPPORTED** |
| 31 | 3.1 Metrics | The overconfidence factor** is how many times larger the typical realised error is than the typical predicted σ: | — | **SUPPORTED** |
| 32 | 3.1 Metrics | A calibrated Gaussian has mean  / σ = $\sqrt{2/\pi}$ = {{v3_rho_calibrated}}. | `v3_metric_definitions.json` | **SUPPORTED** |
| 33 | 3.1 Metrics | The ranking claims (§6.7) are Pearson correlations between the applied scalar penalty and total absolute error, because a ranking claim is about order… | — | **SUPPORTED** |
| 34 | 3.1 Metrics | Horizons.** Curves are reported at $h \in \{1,\,8,\,32,\,{{v2_deploy_h}},\,128,\,{{v2_diag_h}}\}$. | `v2_deployment_horizon.json` | **SUPPORTED** |
| 35 | 3.1 Metrics | h = {{v2_deploy_h}}** is the method's own imagination rollout length — the horizon over which the uncertainty-penalised policy loop actually runs this… | `v2_deployment_horizon.json` | **SUPPORTED** |
| 36 | 3.1 Metrics | h = {{v2_diag_h}}** is the upstream's open-loop diagnostic length: `len_eval_trajectory` = {{v2_len_eval}} minus the {{v2_history}}-step teacher-force… | `v2_deployment_horizon.json` | **SUPPORTED** |
| 37 | 3.1 Metrics | It is {{v2_ratio}}× the method's own rollout length and it is not a deployment horizon; earlier drafts of this paper called it one, and that label is … | `v2_deployment_horizon.json` | **SUPPORTED** |
| 38 | 3.1 Metrics | Both are kept in every table, because h = {{v2_diag_h}} is what makes our numbers comparable to the original's *figure* while h = {{v2_deploy_h}} make… | `v2_deployment_horizon.json` | **SUPPORTED** |
| 39 | 4. What the original paper | Of the {{n_untested_word}} we did not test, **{{appE_n_sim_word}} are claims about policy learning or hardware**: zero-shot transfer, the sample-effic… | `PAPER.template.md, Appendix E + F tables`, `PAPER.template.md, Appendix F table` | **UNREVIEWED** |
| 40 | 4. What the original paper | The remaining {{appE_n_cpu_word}} need none of that and we still did not run them**: the M/N configuration sweep and the MLP/RSSM/transformer baseline… | `PAPER.template.md, Appendix E table` | **UNREVIEWED** |
| 41 | 4. What the original paper | An earlier draft made this a universal claim about all {{n_untested_word}}, which Appendix E contradicts two rows later; the `scope-consistency` check… | `PAPER.template.md, Appendix F table` | **UNREVIEWED** |
| 42 | 4. What the original paper | For all {{orig_n_tested}} of the claims we did test, the original reports no quantitative figure.** Each is asserted qualitatively and shown in a plot… | `original_paper_figures.json` | **SUPPORTED** |
| 43 | 4. What the original paper | So our {{d1_ratio}}× is neither a confirmation of a published figure nor a contradiction of one — it is the first figure attached to the claim, and th… | `task_d1_threeseed.json` | **SUPPORTED** |
| 44 | 4. What the original paper | Where a magnitude is legible only from a plotted curve we say so rather than estimating it from the axis. | — | **SUPPORTED** |
| 45 | 5. The base paper's centra | Which horizon, stated before the result.** The rule below is anchored at h = {{v2_diag_h}}, which §3.1 identifies as the upstream's **open-loop diagno… | `v2_deployment_horizon.json` | **UNREVIEWED** |
| 46 | 5. The base paper's centra | But the paper's own deployment horizon is h = {{v2_deploy_h}}, so the same comparison is reported there too, below, and the two differ in size. | `v2_deployment_horizon.json` | **UNREVIEWED** |
| 47 | 5. The base paper's centra | Three conditions, all required: the out-of-sample gap at h = 368 excludes zero under a bootstrap over independent trajectories; the sign is consistent… | — | **SUPPORTED** |
| 48 | 5. The base paper's centra | Result.** Every condition {{m23_c1}}. | `task5_analysis.json` | **SUPPORTED** |
| 49 | 5. The base paper's centra | The sign test, which does not depend on n.* At h = 368 the per-episode gap favours autoregressive training on **{{c3_sign_pos}} of {{c3_sign_n}}** epi… | `task_c3_multiplicity.json` | **SUPPORTED** |
| 50 | 5. The base paper's centra | Its scope needs stating plainly, because the arena labels invite a stronger reading than it supports.** {{n_train_eps}} of the {{c3_sign_n}} episodes … | `step5_armA_seed0.json (split)`, `task_c3_multiplicity.json` | **SUPPORTED** |
| 51 | 5. The base paper's centra | The out-of-sample effect size below carries that burden, on {{nind_oos_400}} independent trajectories. | `review_bootstrap_unit.json` | **SUPPORTED** |
| 52 | 5. The base paper's centra | The in-sample arena, where the sample is larger.* The same comparison on the eight training episodes has {{nind_ins_400}} independent 400-step traject… | `review_bootstrap_unit.json` | **SUPPORTED** |
| 53 | 5. The base paper's centra | The out-of-sample effect size, over {{d1_seeds}} seeds.* At h = {{v2_diag_h}}, the horizon M-23 is stated over, autoregressive training reaches **{{d1… | `task_d1_threeseed.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 54 | 5. The base paper's centra | At the deployment horizon the same comparison is smaller, and we report it rather than leaving the reader to assume the headline transfers.** At h = {… | `task_d1_threeseed.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 55 | 5. The base paper's centra | M-23's verdict stands as returned at h = {{v2_diag_h}}**; this figure is reported beside it and discharges nothing. | `v2_deployment_horizon.json` | **UNREVIEWED** |
| 56 | 5. The base paper's centra | Seed spread is not symmetric between the arms and that is worth stating: Arm A ranges {{d1_A_lo}}–{{d1_A_hi}} across seeds ({{d1_A_relsd}}% relative),… | `task_d1_threeseed.json` | **SUPPORTED** |
| 57 | 5. The base paper's centra | Teacher forcing is more than twice as variable across seeds as autoregressive training at this horizon, so a single-seed comparison of these two arms … | — | **SUPPORTED** |
| 58 | 5. The base paper's centra | An earlier draft of this paper quoted the single-seed figures {{m23_A}} and {{m23_B}}; those came from the seed that happened to be favourable to Arm … | `task5_analysis.json`, `task_d1_threeseed.json` | **SUPPORTED** |
| 59 | 5. The base paper's centra | For a single seed the bootstrap over trajectories gives 95% interval [{{m23_ci_lo}}, {{m23_ci_hi}}] on n = {{m23_nind}} independent trajectories. | `task5_analysis.json` | **SUPPORTED** |
| 60 | 5. The base paper's centra | That interval should not be read as an ordinary one:** four trajectories admit {{c3_resamples}} distinct resamples, so any bootstrap tail is quantised… | `task_c3_multiplicity.json` | **SUPPORTED** |
| 61 | 5. The base paper's centra | Against a baseline, because neither number means anything without one.* The hold-last floor — predicting that nothing changes — scores **{{floor_h368}… | `task4_arenas.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 62 | 5. The base paper's centra | Autoregressive training beats it by **{{floor_over_A}}×**. | `task4_arenas.json + task5_analysis.json` | **SUPPORTED** |
| 63 | 5. The base paper's centra | Teacher forcing is {{B_over_floor}}× worse than assuming nothing changes at all**, which is the sharper statement of what exposure bias costs here: th… | `task4_arenas.json + task5_analysis.json` | **SUPPORTED** |
| 64 | 5. The base paper's centra | What does not hold, and we say so.** At h = 8 — the horizon the model is trained on — the same comparison out-of-sample gives a gap of {{m23_h8_gap}} … | `task5_analysis.json` | **SUPPORTED** |
| 65 | 5. The base paper's centra | Under the correct cluster bootstrap, the out-of-sample gap excludes zero in **{{ab_long_excl}} of {{ab_long_cells}}** long-horizon cells — both trajec… | `review_bootstrap_unit.json` | **SUPPORTED** |
| 66 | 5. The base paper's centra | Multiplicity.** Those {{ab_long_cells}} cells sit in a family of {{c3_family}} out-of-sample comparisons, so we state the correction rather than leavi… | `review_bootstrap_unit.json`, `task_c3_multiplicity.json` | **SUPPORTED** |
| 67 | 5. The base paper's centra | All {{c3_bonf_excl}} of {{c3_long}} still exclude zero at a Bonferroni level of 0.05/{{c3_family}}, and Holm–Bonferroni rejects **{{c3_holm_rejected}}… | `task_c3_multiplicity.json` | **SUPPORTED** |
| 68 | 5.1 The data budget, which | The base paper's headline is a sample-efficiency result: policies transfer to hardware from {{c2_ref}} state transitions of world-model pretraining ag… | `task_c2_data_budget.json` | **SUPPORTED** |
| 69 | 5.1 The data budget, which | Our arms consume {{c2_trans}} distinct state transitions.** That is the {{c2_rows}} rows of the eight training episodes less one per episode boundary … | `task_c2_data_budget.json` | **SUPPORTED** |
| 70 | 5.1 The data budget, which | It is deliberately not the {{c2_windows}} training windows, which overlap almost completely — consecutive 33-step windows start one row apart — nor th… | `task_c2_data_budget.json` | **SUPPORTED** |
| 71 | 5.1 The data budget, which | Against the reference's {{c2_ref}}, that is **{{c2_ratio}}× less data, {{c2_pct}}% of its world-model budget**. | `task_c2_data_budget.json` | **SUPPORTED** |
| 72 | 5.1 The data budget, which | A dynamics model trained on {{c2_pct}}% of the reference's data still reproduces the autoregressive-versus-teacher-forcing result at {{d1_ratio}}× and… | `task4_arenas.json + task5_analysis.json`, `task_c2_data_budget.json`, `task_d1_threeseed.json` | **SUPPORTED** |
| 73 | 5.1 The data budget, which | Three limits, in the same breath.** It is not a reproduction of the {{c2_ref}}-against-250M comparison, which is about policy learning and which we do… | `task_c2_data_budget.json` | **SUPPORTED** |
| 74 | 5.1 The data budget, which | And our model is evaluated on the same narrow distribution it trained on — one robot, one gait, one terrain, velocity commands from a single bounded b… | `task_c2_data_budget.json` | **SUPPORTED** |
| 75 | 6.1 Which quantity the met | The paper agrees with its code. arXiv:2504.16680**v1** Eq. 4 defines the penalised quantity as — the numbering is unchanged in the current {{v4_curren… | `original_paper_figures.json` | **SUPPORTED** |
| 76 | 6.1 Which quantity the met | We asked, and the first author confirms the code is operative: the penalty is applied to the standard deviation as intended, and Eq. 4 is "more of a h… | — | **SUPPORTED** |
| 77 | 6.1 Which quantity the met | It is reported in Fig. 3 (right) as an analysis of the model behavior." (The figure number is the first author's and follows arXiv:2504.16680**v1**; i… | `original_paper_figures.json` | **SUPPORTED** |
| 78 | 6.1 Which quantity the met | The crosswalk is in `results/original_paper_figures.json`.) So what follows is not an implementation slip being reported back to its authors — it is t… | — | **SUPPORTED** |
| 79 | 6.1 Which quantity the met | It is three things the papers do not address: that the aleatoric head is discarded before use, that neither quantity is usable as a scale, and that th… | — | **SUPPORTED** |
| 80 | 6.2 The measurement | A calibrated Gaussian puts {{v3_cov_nominal1}}% inside ±1σ (§3.1). | `v3_metric_definitions.json` | **SUPPORTED** |
| 81 | 6.2 The measurement | Every cell carries a 95% interval from a cluster bootstrap over whole trajectories, n_independent = {{b2_nind}}; where three seeds contribute, seeds a… | `task_b2_epistemic.json` | **SUPPORTED** |
| 82 | 6.2 The measurement | At n_independent = {{b2_nind}} the bootstrap has {{c3_resamples}} distinct resamples and the intervals are quantised at that resolution. | `task_b2_epistemic.json`, `task_c3_multiplicity.json` | **SUPPORTED** |
| 83 | 6.2 The measurement | All four rows are the held-out arena** — the two episodes withheld from our own arms, n_independent = {{b2_nind}} — because that is the only arena on … | `task_b2_epistemic.json` | **SUPPORTED** |
| 84 | 6.2 The measurement | On the aleatoric head every model is overconfident by between one and four orders of magnitude (Figure 1); that is the quantity §6.1 shows the method … | — | **SUPPORTED** |
| 85 | 6.2 The measurement | A note on the released checkpoint's row, so the next table does not read as a contradiction.* Its {{cal_rel_ratio}}× is the whole {{v2_diag_h}}-step r… | `task1_calibration.json`, `task_b2_epistemic.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 86 | 6.2 The measurement | The released checkpoint trained on all ten episodes, so its own best-sampled figure is the {{d1n_alea_ratio_h100}}× below — cumulative to h = {{v2_dep… | `task_d_nind20.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 87 | 6.2 The measurement | The quantity the method does use is also uncalibrated.** On the released {{b2_members}}-member checkpoint over all {{d1n_eps}} episodes, n_independent… | `task_b2_epistemic.json`, `task_d_nind20.json` | **SUPPORTED** |
| 88 | 6.2 The measurement | We use all ten rather than the held-out pair here because the released checkpoint trained on all ten, so restricting it to two buys no independence an… | — | **SUPPORTED** |
| 89 | 6.2 The measurement | The held-out-only version at n_independent = {{b2_nind}} is in the supplementary material (`results/task_b2_epistemic.json`). | `task_b2_epistemic.json` | **SUPPORTED** |
| 90 | 6.2 The measurement | The epistemic column agrees in direction with this one at all {{agree_epi}} of {{agree_nh}} horizons; the aleatoric column agrees at {{agree_alea}} of… | `task_b2_epistemic.json + task_d_nind20.json` | **SUPPORTED** |
| 91 | 6.2 The measurement | At h = {{v2_deploy_h}}, the method's own imagination rollout length, epistemic is {{d1n_epi_over_alea_h100}}× better than aleatoric and still wrong by… | `task_d_nind20.json`, `v2_deployment_horizon.json`, `v3_metric_definitions.json` | **UNREVIEWED** |
| 92 | 6.2 The measurement | At one step it is **{{d1n_epi_ratio_h1}}×** out and {{d1n_epi_over_alea_h1}}× better than aleatoric; at h = {{v2_diag_h}} the two-term ratio is {{d1n_… | `task_d_nind20.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 93 | 6.2 The measurement | The gap between the two uncertainty terms is itself horizon-dependent, which is why each figure above names its horizon.** At the open-loop diagnostic… | `task_d_nind20.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 94 | 6.2 The measurement | The larger sample changes one thing materially, and it is a correction to our own earlier reading.** At n_independent = {{b2_nind}} the epistemic orde… | `task_b2_epistemic.json` | **SUPPORTED** |
| 95 | 6.2 The measurement | At n_independent = {{d1n_nind}} it is {{d1n_epi_npos_h1}} of {{d1n_epi_ndim_h1}} at h=1, with mean r = {{d1n_epi_r_h1}}, the *strongest* mean correlat… | `task_d_nind20.json` | **SUPPORTED** |
| 96 | 6.2 The measurement | The short-horizon "chance" result was an artifact of four trajectories, not a property of the model, and we record it as such rather than keeping the … | — | **SUPPORTED** |
| 97 | 6.2 The measurement | The released checkpoint is no longer the only ensemble measured.** Three Arm A arms at ensemble size 5 (§6.7, {{e5_seeds}} seeds, out-of-sample, n_ind… | `task_d3_ens5.json` | **SUPPORTED** |
| 98 | 6.2 The measurement | Our arms are **better calibrated than the released checkpoint and fail the same way**: {{e5_ratio_h100}}× overconfident at h = {{v2_deploy_h}} against… | `task_d3_ens5.json`, `task_d_nind20.json`, `v2_deployment_horizon.json`, `v3_metric_definitions.json` | **SUPPORTED** |
| 99 | 6.2 The measurement | The last column of the table above gives permutation P-values over whole trajectories, not binomial ones, computed on the same {{perm_all_nind}} traje… | `task_b_permutation.json` | **SUPPORTED** |
| 100 | 6.2 The measurement | h = {{v2_deploy_h}} carries the abstract, so it is tested rather than left blank**: an earlier draft ran the permutation test on the pre-revision five… | `v2_deployment_horizon.json` | **UNREVIEWED** |
| 101 | 6.2 The measurement | These are {{perm_n_tests_col_word}} tests on one family and none survives Holm–Bonferroni across the arena's {{perm_all_holm_n}} cells — the smallest … | `task_b_permutation.json` | **UNREVIEWED** |
| 102 | 6.2 The measurement | Read the column as a consistency check on direction, not as {{perm_n_tests_col_word}} independent findings. | `task_b_permutation.json` | **UNREVIEWED** |
| 103 | 6.2 The measurement | The scalar penalty as actually applied — `means.std(0).sum(-1)` at `envs/base.py:166` — correlates **{{d4_r}}** with total absolute error over the rol… | `task_d_nind20.json` | **SUPPORTED** |
| 104 | 6.3 Why the aleatoric head | Across all {{n_runs}} runs the collapse is linear in iteration count and its rate is nearly identical (Figure 3a). | `step5_arm*.json` | **SUPPORTED** |
| 105 | 6.3 Why the aleatoric head | Rates are fitted on {{e2_fitted_runs}} of those runs: the {{e2_excluded_10k}} 10,000-iteration runs are excluded from the rate statistics because they… | `step5_arm*.json` | **SUPPORTED** |
| 106 | 6.3 Why the aleatoric head | Figure 3(a) shows all {{run_total}} runs; Figure 3(b) plots only the {{e2_fitted_runs}} the rate is fitted on, so the scatter and the quoted statistic… | `step5_arm*.json` | **SUPPORTED** |
| 107 | 6.3 Why the aleatoric head | The {{run_total}} runs, so a reader can count them: | `step5_arm*.json` | **SUPPORTED** |
| 108 | 6.3 Why the aleatoric head | Two different things are being explained here, and §6.6 separates them.** *Magnitude collapse is objective-driven.* It occurs in all {{e2_mse_runs}} s… | `step5_arm*.json` | **SUPPORTED** |
| 109 | 6.3 Why the aleatoric head | Input-independence is not.* That varies by a factor of {{cal_armB_over_faithA_cov}} between two arms trained under the same objective, so the objectiv… | `task1_calibration.json` | **SUPPORTED** |
| 110 | 6.4 Why the epistemic term | The state pathway is a {{v1_shared_params}}-parameter two-layer GRU trunk plus {{v1_members_word}} heads of {{v1_private_params}} parameters each. | `v1_ensemble_topology.json` | **SUPPORTED** |
| 111 | 6.4 Why the epistemic term | Per member, {{v1_shared_params}} of {{v1_member_params}} parameters — **{{v1_shared_pct}}%** — are numerically identical to every other member's. | `v1_ensemble_topology.json` | **SUPPORTED** |
| 112 | 6.4 Why the epistemic term | Only {{v1_private_pct}}% differ. | `v1_ensemble_topology.json` | **SUPPORTED** |
| 113 | 6.4 Why the epistemic term | Across the whole released object, the two shared trunks are {{v1_shared_pct_model}}% of {{v1_total_params}} parameters. | `v1_ensemble_topology.json` | **SUPPORTED** |
| 114 | 6.4 Why the epistemic term | The sharing is stronger than the parameter count suggests, and this is the part that matters.** The trunk owns a *single* recurrent hidden state (`rnn… | — | **SUPPORTED** |
| 115 | 6.4 Why the epistemic term | So the {{v1_members_word}} members do not roll out independently at all. | `v1_ensemble_topology.json` | **SUPPORTED** |
| 116 | 6.4 Why the epistemic term | There is exactly {{v1_hidden_states_word}} hidden-state trajectory between them, and disagreement at step *t* is the spread of {{v1_members_word}} two… | `v1_ensemble_topology.json` | **SUPPORTED** |
| 117 | 6.4 Why the epistemic term | Members which share a feature extractor have correlated errors by construction, and their spread cannot express uncertainty the shared trunk does not … | — | **SUPPORTED** |
| 118 | 6.4 Why the epistemic term | Their tensor names and parameter counts match the released checkpoint exactly — {{v1_shared_params}} shared, {{v1_private_params}} per head, {{v1_tota… | `v1_ensemble_topology.json` | **SUPPORTED** |
| 119 | 6.4 Why the epistemic term | So §6.2's "our arms fail the same way at {{e5_ratio_h100}}× at h = {{v2_deploy_h}}" compares two instances of one architecture, not two architectures.… | `task_d3_ens5.json`, `v1_ensemble_topology.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 120 | 6.5 The correction fails d | Running it reverses the collapse and improves the magnitude from {{cal_faithA_ratio}}× to {{cal_nll_ratio}}× overconfident. | `task1_calibration.json` | **SUPPORTED** |
| 121 | 6.5 The correction fails d | It does not produce a usable estimate, and it destroys something the faithful arm had: the σ-versus-error ordering falls from {{cal_faithA_npos}}/{{ca… | `task1_calibration.json` | **SUPPORTED** |
| 122 | 6.5 The correction fails d | Under the trajectory permutation test of §6.6, at h = {{v2_diag_h}}, those counts give P = {{perm_oos_faithA_p_h368}} and {{perm_oos_nll_p_h368}} out … | `task_b_permutation.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 123 | 6.6 The failure is one of  | Our ensemble-5 arms have both: their aleatoric CoV is comparable to the other arms', and their *epistemic* term is far more input-dependent than any a… | `task1_calibration.json`, `task_d3_ens5.json` | **SUPPORTED** |
| 124 | 6.6 The failure is one of  | The count column is the out-of-sample arena** (n_independent = {{relale_oos_nind}}), so that all four models are compared on trajectories none of our … | `task_b_permutation.json` | **SUPPORTED** |
| 125 | 6.6 The failure is one of  | It is not the only arena, and for the released checkpoint's aleatoric head it is not the most informative one: at h = {{v2_diag_h}} and n_independent … | `task_b_permutation.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 126 | 6.6 The failure is one of  | Arm B's σ is {{cal_armB_over_faithA_cov}}× more input-dependent than the faithful arm's, and it has the largest mean correlation of the four (r = {{ca… | `task1_calibration.json` | **SUPPORTED** |
| 127 | 6.6 The failure is one of  | It is still {{cal_armB_ratio}}× overconfident. | `task1_calibration.json` | **SUPPORTED** |
| 128 | 6.6 The failure is one of  | More importantly, error grows with rollout depth in every trajectory, so *any* σ that also grows with depth correlates with *any* trajectory's error —… | — | **SUPPORTED** |
| 129 | 6.6 The failure is one of  | The worst-affected cell is {{perm_worst_model}} at h={{perm_worst_h}}, in the {{perm_worst_arena}} arena. | `task_b_permutation.json` | **SUPPORTED** |
| 130 | 6.6 The failure is one of  | At h = {{v2_diag_h}} it moves from {{perm_ins_armB_binom_h368}} to {{perm_ins_armB_p_h368}} — a factor of about {{perm_worst_factor}} — because under … | `task_b_permutation.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 131 | 6.6 The failure is one of  | Observing {{perm_ins_armB_npos_h368}} of {{perm_ins_armB_ndim_h368}} against that null is close to unremarkable. | `task_b_permutation.json` | **SUPPORTED** |
| 132 | 6.6 The failure is one of  | A fair coin, by contrast, centres the count at 22.5 of 45; the dependence-preserving null centres it between {{perm_null_lo}} and {{perm_null_hi}} dep… | `task_b_permutation.json` | **SUPPORTED** |
| 133 | 6.6 The failure is one of  | The teacher-forced arm collapses in magnitude exactly like the autoregressive ones — same objective, same rate — while retaining {{cal_armB_over_faith… | `task1_calibration.json` | **SUPPORTED** |
| 134 | 6.6 The failure is one of  | The same pattern holds for the quantity the method uses, and this is where the correction bites hardest.** At h=128 and h=368 the epistemic term corre… | `task_b2_epistemic.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 135 | 6.6 The failure is one of  | All figures in this paragraph are the held-out arena (n_independent = {{b2_nind}})**, so that the epistemic term and the four aleatoric heads are comp… | `task_b2_epistemic.json`, `task_d_nind20.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 136 | 6.6 The failure is one of  | The figure the abstract and §13 use is neither of those: it is {{d1n_epi_ratio_h100}}× at h = {{v2_deploy_h}}, on the same {{d1n_nind}} trajectories. | `task_d_nind20.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 137 | 6.6 The failure is one of  | It does not beat Arm B's head on strength either: its mean correlation at h=368 is {{b2_epi_r_h368}} against {{cal_armB_r}}. | `task1_calibration.json`, `task_b2_epistemic.json` | **SUPPORTED** |
| 138 | 6.6 The failure is one of  | Under the permutation null that count gives P = {{perm_oos_epi_p_h368}} out of sample and {{perm_ins_epi_p_h368}} in sample, against {{perm_oos_epi_bi… | `task_b_permutation.json` | **SUPPORTED** |
| 139 | 6.6 The failure is one of  | It still fails the horizon test the same way: σ grows {{b2_epi_sigma_growth}}× from h=1 to h=368 while error grows {{b2_epi_err_growth}}×. | `task_b2_epistemic.json` | **SUPPORTED** |
| 140 | 6.6 The failure is one of  | The horizon story we first told was backwards, and the larger arenas agree with each other against the smallest.** At n_independent = {{perm_oos_nind}… | `task_b_permutation.json` | **SUPPORTED** |
| 141 | 6.6 The failure is one of  | In sample (n_independent = {{perm_ins_nind}}): {{perm_ins_epi_p_h1}} at h=1, {{perm_ins_epi_p_h8}} at h=8, against {{perm_ins_epi_p_h128}} at h=128. | `task_b_permutation.json` | **SUPPORTED** |
| 142 | 6.6 The failure is one of  | Over all ten episodes (n_independent = {{perm_all_nind}}): {{perm_all_epi_p_h1}}, {{perm_all_epi_p_h8}} and {{perm_all_epi_p_h128}}. | `task_b_permutation.json` | **SUPPORTED** |
| 143 | 6.6 The failure is one of  | At long horizon the shared forecast-depth trend lifts the null to {{perm_all_epi_null_h128}} of 45 at h = 128, so a count of 45 is close to what chanc… | `task_b_permutation.json` | **UNREVIEWED** |
| 144 | 6.6 The failure is one of  | The out-of-sample arena is not wrong so much as blind: at {{perm_oos_nind}} trajectories its smallest attainable P-value is {{perm_oos_floor}}, so it … | `task_b_permutation.json` | **SUPPORTED** |
| 145 | 6.6 The failure is one of  | We report the small arena's numbers alongside because it is the only arena that is out-of-sample for our own arms, not because it is the better measur… | — | **SUPPORTED** |
| 146 | 6.6 The failure is one of  | Nothing here survives multiplicity correction, in any of the three arenas.** Holm–Bonferroni over each arena's {{perm_oos_holm_n}} model × horizon cel… | `task_b_permutation.json` | **SUPPORTED** |
| 147 | 6.6 The failure is one of  | Out of sample that is a property of the design rather than of the models: with {{perm_oos_nind}} independent trajectories the smallest attainable P-va… | `task_b_permutation.json` | **SUPPORTED** |
| 148 | 6.6 The failure is one of  | In sample the miss is real — the smallest P in the family is {{perm_ins_holm_min_cell}} at {{perm_ins_holm_min_p}} against a threshold of {{perm_ins_h… | `task_b_permutation.json` | **SUPPORTED** |
| 149 | 6.6 The failure is one of  | So the honest form of this section's claim is narrower than the one we first wrote. | — | **SUPPORTED** |
| 150 | 6.7 Ensemble disagreement  | Section 6.6 shows the per-dimension version of that claim is weaker than it looks. | — | **SUPPORTED** |
| 151 | 6.7 Ensemble disagreement  | This section asks a different and, for a practitioner, more important question: **does disagreement beat something free? | — | **SUPPORTED** |
| 152 | 6.7 Ensemble disagreement  | All three correlations below are on the scalar quantity the method actually applies — `means.std(0).sum(-1)` at `envs/base.py:166` — against total abs… | `task_d_nind20.json` | **SUPPORTED** |
| 153 | 6.7 Ensemble disagreement  | Disagreement wins at every horizon tested.** Over the full h = {{v2_diag_h}} rollout the counter reaches {{d2b_idx_h368}} against disagreement's {{d2b… | `task_d_nind20.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 154 | 6.7 Ensemble disagreement  | The paired difference — resampling whole trajectories and recomputing *both* correlations inside each draw — is the appropriate test and the more powe… | — | **SUPPORTED** |
| 155 | 6.7 Ensemble disagreement  | It excludes zero at **{{d2p_n_separating}} of {{d2p_n_horizons}}** horizons. | `task_d_nind20.json` | **SUPPORTED** |
| 156 | 6.7 Ensemble disagreement  | The distinction matters at {{d2p_n_overlap}} of the {{d2p_n_horizons}} horizons. | `task_d_nind20.json` | **UNREVIEWED** |
| 157 | 6.7 Ensemble disagreement  | At {{d2p_overlap_h}} the marginal intervals *do* overlap, and an earlier draft of this paper wrongly asserted that they never do. {{d2b_idx_strongest_… | `task_d_nind20.json` | **UNREVIEWED** |
| 158 | 6.7 Ensemble disagreement  | At h = {{v2_deploy_h}} the paired difference is {{d2p_diff_h100}} {{d2p_ci_h100}}, which also excludes zero. | `task_d_nind20.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 159 | 6.7 Ensemble disagreement  | The third column answers a different question: is disagreement merely re-encoding the clock?** Partialling the step index out of both variables *lower… | `task_d_nind20.json` | **SUPPORTED** |
| 160 | 6.7 Ensemble disagreement  | Partialling out log(1 + index) gives {{d2r_log}} {{d2r_log_ci}}; a cubic in the index gives {{d2r_cub}} {{d2r_cub_ci}}; a rank partial correlation, wh… | `task_d2b_robustness.json` | **SUPPORTED** |
| 161 | 6.7 Ensemble disagreement  | Computing the correlation **within each forecast step** — across trajectories, with depth held exactly constant, so the index cannot contribute by con… | `task_d2b_robustness.json` | **SUPPORTED** |
| 162 | 6.7 Ensemble disagreement  | [^stepcount]: Adjacent forecast steps on the same {{a2_nind}} trajectories are heavily dependent — structurally the same problem §6.6 spends a page co… | `a2_trajectory_level_control.json` | **SUPPORTED** |
| 163 | 6.7 Ensemble disagreement  | The count is descriptive; the interval {{d2r_win_ci}} is the statistic, and no P-value attaches to {{d2r_win_pos}}/{{d2r_win_n}}. | `task_d2b_robustness.json` | **SUPPORTED** |
| 164 | 6.7 Ensemble disagreement  | Per-episode difficulty in this dataset spans {{d12_lo}} to {{d12_hi}} and is uncorrelated with commanded speed, so the units being correlated differ a… | `a2_trajectory_level_control.json` | **SUPPORTED** |
| 165 | 6.7 Ensemble disagreement  | If harder trajectories simply have both larger error and larger disagreement, the pooled correlation would look exactly as it does with disagreement c… | — | **SUPPORTED** |
| 166 | 6.7 Ensemble disagreement  | Two things suggested that was worth checking: the {{a2_h1_npoints}}-point h = 1 figure of {{a2_h1_r}}, which is not the shape of a genuine per-step si… | `a2_trajectory_level_control.json` | **SUPPORTED** |
| 167 | 6.7 Ensemble disagreement  | The between-trajectory correlation is **{{a2_r_between}}**, and the two components contribute {{a2_share_between}}% and {{a2_share_within}}% of the po… | `a2_trajectory_level_control.json` | **SUPPORTED** |
| 168 | 6.7 Ensemble disagreement  | That reinterprets the fifth control rather than merely adding to it.** The within-step statistic correlates *across trajectories* at each fixed step a… | — | **SUPPORTED** |
| 169 | 6.7 Ensemble disagreement  | It is a mean of {{d2r_win_n}} between-trajectory correlations, which is why it reads {{d2r_win}} against the pooled {{a2_r_pooled}} rather than below … | `a2_trajectory_level_control.json`, `task_d2b_robustness.json` | **SUPPORTED** |
| 170 | 6.7 Ensemble disagreement  | The decisive statistic is the double-demeaned one, and it survives.** Removing the trajectory mean *and* the step mean from both variables and correla… | `a2_trajectory_level_control.json` | **SUPPORTED** |
| 171 | 6.7 Ensemble disagreement  | The rule's minimum detectable effect at this sample size is an absolute r_dd of {{p1_m45_mde}} or more, estimated by a dilution study which put the de… | `p1_power_check.json` | **UNREVIEWED** |
| 172 | 6.7 Ensemble disagreement  | M-45 returns {{m45_verdict}}**: with both the rollout and the depth held constant, disagreement still tracks error. | `a2_trajectory_level_control.json` | **SUPPORTED** |
| 173 | 6.7 Ensemble disagreement  | Per horizon, on the same {{a2_nind}} trajectories and the same cluster bootstrap: | `a2_trajectory_level_control.json` | **UNREVIEWED** |
| 174 | 6.7 Ensemble disagreement  | (h = 1 has one forecast step per trajectory, so there is nothing within a rollout to demean against and r_dd is undefined rather than zero.) | — | **UNREVIEWED** |
| 175 | 6.7 Ensemble disagreement  | The within-rollout effect is **materially smaller than the pooled figure** — {{a2_rdd}} against {{a2_r_pooled}} — so a practitioner should expect disa… | `a2_trajectory_level_control.json` | **SUPPORTED** |
| 176 | 6.7 Ensemble disagreement  | And it is **not established at short horizon**: r_dd's interval excludes zero at {{a2_excl_h}} and spans zero at {{a2_spans_h}}, where too few steps e… | `a2_trajectory_level_control.json` | **SUPPORTED** |
| 177 | 6.7 Ensemble disagreement  | At h = {{v2_deploy_h}} it is established, at {{a2_rdd_h100}} {{a2_rdd_ci_h100}}. | `a2_trajectory_level_control.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 178 | 6.7 Ensemble disagreement  | The h = 1 figure survives the same test, but it is not what it looked like.** At h = 1 the panel has one column, so {{a2_h1_r}} is a correlation over … | `a2_trajectory_level_control.json` | **SUPPORTED** |
| 179 | 6.7 Ensemble disagreement  | We checked whether trajectory difficulty manufactures it: disagreement correlates {{a2_h1_speed_r}} with commanded speed and {{a2_h1_diff_r}} with per… | `a2_trajectory_level_control.json` | **SUPPORTED** |
| 180 | 6.7 Ensemble disagreement  | It is nevertheless a statement about **ranking whole rollouts at one step ahead**, on {{a2_h1_npoints}} points, and §10 now says that rather than call… | `a2_trajectory_level_control.json` | **SUPPORTED** |
| 181 | 6.7 Ensemble disagreement  | It returns {{e5_verdict}}.** The first condition passes completely — disagreement leads the index in **{{e5_lead_cells}} of {{e5_total_cells}}** seed-… | `task_d3_ens5.json` | **SUPPORTED** |
| 182 | 6.7 Ensemble disagreement  | The second fails: the paired difference excludes zero at {{e5_n_excl}} of {{e5_n_horizons}} horizons, not a majority. | `task_d3_ens5.json` | **SUPPORTED** |
| 183 | 6.7 Ensemble disagreement  | What separates the two conditions is sample size, and we measured that rather than asserting it.** Our own arms can only be scored out-of-sample on th… | `task_d3_ens5.json`, `task_d_nind20.json` | **SUPPORTED** |
| 184 | 6.7 Ensemble disagreement  | Subsampling four trajectories at a time from a twenty-trajectory pool, the rule's criterion fires on {{e5_power_mean}}% of draws on average and on onl… | `task_d3b_ens5_power.json` | **SUPPORTED** |
| 185 | 6.7 Ensemble disagreement  | That estimate is an **upper bound**, because the pool it subsamples is in-sample for these arms, where the effect is {{e5_eff_ins}} against {{e5_eff_o… | `task_d3b_ens5_power.json` | **SUPPORTED** |
| 186 | 6.7 Ensemble disagreement  | Reported as a companion and not as a discharge:* on all ten episodes (n_independent = {{e5_comp_nind}}, **in-sample** for these arms, which trained on… | `task_d3b_ens5_power.json` | **SUPPORTED** |
| 187 | 6.7 Ensemble disagreement  | It cannot discharge M-43, which is stated over the out-of-sample arena, and we record it only so the comparison with the released checkpoint's {{d1n_n… | `task_d_nind20.json` | **SUPPORTED** |
| 188 | 6.7 Ensemble disagreement  | We ran the baseline test expecting it to go the other way.** A counter matching disagreement would have been the more consequential result — it would … | — | **SUPPORTED** |
| 189 | 6.8 One constant scalar do | On the epistemic term — the quantity the method uses — a scalar of {{d2_epi_c_lo}}–{{d2_epi_c_hi}} brings h=1 coverage to {{d2_epi_cov1_lo}}–{{d2_epi_… | `task_d2_recalibration.json`, `v3_metric_definitions.json` | **SUPPORTED** |
| 190 | 6.8 One constant scalar do | On the aleatoric term a scalar of {{d2_ale_c_lo}}–{{d2_ale_c_hi}} gives {{d2_ale_cov1_lo}}–{{d2_ale_cov1_hi}}% at h=1 and {{d2_ale_cov368_lo}}–{{d2_al… | `task_d2_recalibration.json` | **SUPPORTED** |
| 191 | 6.8 One constant scalar do | The two held-out episodes contribute {{d3_nind_tot}} non-overlapping 400-step trajectories between them, so each direction fits on n_independent = {{d… | `task_d3_perhorizon.json` | **SUPPORTED** |
| 192 | 6.8 One constant scalar do | Every held-out cell lands within {{d3_tol}} points of the {{d3_target}}% target for both quantities. | `task_d3_perhorizon.json` | **SUPPORTED** |
| 193 | 6.8 One constant scalar do | The largest deviation over all {{d3_ncells_all}} held-out cells is {{d3_worst_q}} at h={{d3_worst_h}}, fitted on episode {{d3_worst_ep}} and scored on… | `task_d3_perhorizon.json` | **SUPPORTED** |
| 194 | 6.8 One constant scalar do | The two largest deviations are both on the {{d3_second_q}} term and {{d3_top2_same_side}} target — {{d3_worst_cov}}% at h={{d3_worst_h}} and {{d3_seco… | `task_d3_perhorizon.json` | **UNREVIEWED** |
| 195 | 6.8 One constant scalar do | The constant scalar manages {{d3_epi_const_ok}} of {{d3_epi_cells}}, and those are the h=1 cells it was fitted at. | `task_d3_perhorizon.json` | **SUPPORTED** |
| 196 | 6.8 One constant scalar do | The per-horizon scalar has one free parameter per horizon against the constant one's one, so it *must* fit better in sample — only the held-out column… | — | **SUPPORTED** |
| 197 | 6.8 One constant scalar do | And the held-out column is thinner than its count suggests:** the {{d3_epi_cells}} cells are {{d3_nhoriz}} horizons × two fold directions on the same … | `task_d3_perhorizon.json` | **SUPPORTED** |
| 198 | 6.8 One constant scalar do | They are not {{d3_epi_cells}} independent successes and no P-value attaches to the count; it is reported so a reader can see how thin the evidence is,… | `task_d3_perhorizon.json` | **SUPPORTED** |
| 199 | 6.8 One constant scalar do | And the correction is a calibration patch, not a fix: it leaves the model's σ carrying no more information than before and simply rescales it by how f… | — | **SUPPORTED** |
| 200 | 6.9 The structural excuse  | The faithful arm's σ *declines* ({{sig_faithA_growth}}×) while its error grows {{err_faithA_growth}}×. | `task2_sigma_profile.json` | **SUPPORTED** |
| 201 | 6.10 Testing the mechanism | Arm A at ensemble size 1 already existed at seeds 0, 1 and 2; we added seeds 3 and 4 at about 1.2 h each and scored the {{r2_n_indep}} together as an … | `r2_independent_ensemble.json` | **SUPPORTED** |
| 202 | 6.10 Testing the mechanism | No new training code and no new architecture — and the disagreement across {{r2_n_indep}} independently-initialised *full models* is exactly the contr… | `r2_independent_ensemble.json` | **SUPPORTED** |
| 203 | 6.10 Testing the mechanism | Same trajectories, same harness, n_independent = {{r2_nind}}. | `r2_independent_ensemble.json` | **SUPPORTED** |
| 204 | 6.10 Testing the mechanism | The shared-trunk column is the mean over {{r2_n_shared}} seeds; the comparison below is paired against each of them separately. | `r2_independent_ensemble.json` | **SUPPORTED** |
| 205 | 6.10 Testing the mechanism | M-44 returns {{m44_verdict}}.** All {{m44_n_conditions_met}} of its {{m44_n_conditions}} conditions hold, against every one of the {{r2_n_shared}} sha… | `r2_independent_ensemble.json` | **SUPPORTED** |
| 206 | 6.10 Testing the mechanism | At h = {{v2_deploy_h}} the independent ensemble's overconfidence factor is {{r2_ratio_lo}}–{{r2_ratio_hi}}× the shared-trunk arms' — a **{{m44_ratio_g… | `r2_independent_ensemble.json`, `v2_deployment_horizon.json` | **SUPPORTED** |
| 207 | 6.10 Testing the mechanism | Members that share a feature extractor do produce a smaller spread, and the effect is large enough to matter. | — | **SUPPORTED** |
| 208 | 6.10 Testing the mechanism | Five independent models also denoise better than five heads on one trunk, which is an ordinary ensembling effect and not the thing under test. | — | **SUPPORTED** |
| 209 | 6.10 Testing the mechanism | A share above 100% at h=1 means the independent ensemble is very slightly the **worse** predictor there and the σ gain more than covers it. | — | **SUPPORTED** |
| 210 | 6.10 Testing the mechanism | The reading, stated at the horizon the rule is stated over.** σ is larger at every horizon — by {{r2_sigma_x_lo}}× at its weakest (h = {{r2_sigma_x_lo… | `r2_independent_ensemble.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 211 | 6.10 Testing the mechanism | So the mechanism is supported and it is the larger part of the effect where the method operates. | — | **SUPPORTED** |
| 212 | 6.10 Testing the mechanism | At the open-loop diagnostic horizon of h = {{v2_diag_h}} the split reverses — {{r2_from_acc_h368}}% of the improvement there is the ensemble simply pr… | `r2_independent_ensemble.json`, `v2_deployment_horizon.json` | **SUPPORTED** |
| 213 | 6.10 Testing the mechanism | We report both columns for that reason. (An earlier draft gave the σ range above as the h = 1 and h = 8 values, which do not span it — h = {{r2_sigma_… | `r2_independent_ensemble.json` | **UNREVIEWED** |
| 214 | 6.10 Testing the mechanism | It does not license attributing the whole gap to trunk-sharing: §12 sets out that independently-seeded runs differ in *both* initialisation and data o… | — | **SUPPORTED** |
| 215 | 6.10 Testing the mechanism | And it does not repair the interval.** The independent ensemble is {{r2_indep_ratio_h100}}× overconfident at h = {{v2_deploy_h}} with {{r2_indep_cov1_… | `r2_independent_ensemble.json`, `v2_deployment_horizon.json`, `v3_metric_definitions.json` | **SUPPORTED** |
| 216 | 6.10 Testing the mechanism | Better by a factor of {{m44_ratio_gain}}, and still not an interval. | `r2_independent_ensemble.json` | **SUPPORTED** |
| 217 | 7. Defects in the released | The window builder reads a termination column that is identically zero, so it marks all {{win_naive}} windows valid. | `step0_regimes.json` | **SUPPORTED** |
| 218 | 7. Defects in the released | Scored correctly the released checkpoint is materially better than its own released evaluation reports: nRMSE at h = 368 falls from {{stale_nrmse}} un… | `step4_0a_results.json` | **SUPPORTED** |
| 219 | 7. Defects in the released | 7.4 What the spliced windows cost: nothing measurable.** We trained a contaminated arm on {{arm_contam_windows}} windows — the clean {{arm_clean_windo… | `step5_armA_seed0.json`, `step5_armA_seed0_contam.json` | **SUPPORTED** |
| 220 | 7. Defects in the released | The arm's contamination rate is {{arm_contam_pct}}%, against the reference pipeline's {{contam_pct}}%. | `step0_regimes.json`, `step5_armA_seed0_contam.json` | **SUPPORTED** |
| 221 | 7. Defects in the released | It is deliberately lower: we splice only the {{bound_both_train}} boundaries whose *both* sides are training episodes, because {{bound_touch_holdout}}… | `step5_armA_seed0.json (split)` | **SUPPORTED** |
| 222 | 7. Defects in the released | That is a leakage problem rather than a physics one, and including it would have invalidated our own comparison. | — | **SUPPORTED** |
| 223 | 7. Defects in the released | Training loss over the final 250 iterations: duplication costs {{dup_cost_pct}}%, splicing costs {{contam_cost_pct}}%. | `task3_control_arm.json` | **SUPPORTED** |
| 224 | 7. Defects in the released | The bootstrap interval on duplicated − clean is [{{dup_ci_lo}}, {{dup_ci_hi}}], including zero. | `task3_control_arm.json` | **SUPPORTED** |
| 225 | 7. Defects in the released | In rollout, across {{tw_cells}} cells (two arenas × two trajectory lengths × two checkpoints × two horizons × two metrics), contamination hurts in **{… | `task3_three_way.json` | **SUPPORTED** |
| 226 | 7. Defects in the released | The control is inert, differing from clean in {{tw_dc_cluster_helped}} cells. | `task3_three_way.json` | **SUPPORTED** |
| 227 | 7. Defects in the released | "Costs nothing" is the wrong summary, and we should not use it.** Splicing raises training loss by {{contam_cost_pct}}% against duplication's {{dup_co… | `task3_control_arm.json`, `task3_three_way.json` | **SUPPORTED** |
| 228 | 7. Defects in the released | Both are measured effects in opposite directions, which is the signature of regularisation: the spliced windows contain transitions the model cannot f… | — | **SUPPORTED** |
| 229 | 8. The released checkpoint | Fitting it across our runs and extrapolating to the released checkpoint's σ state implies **{{implied_iters}}** optimisation steps at the configured l… | `step6_analysis.json` | **SUPPORTED** |
| 230 | 8. The released checkpoint | The refit from our 10,000-iteration runs gives {{q4_implied_A}} and {{q4_implied_B}}, spreading {{implied_spread_pct}}% across the three fits — a line… | `task5_analysis.json`, `step6_analysis.json` | **SUPPORTED** |
| 231 | 8. The released checkpoint | So the defensible claim is narrower than "cannot have come from the released recipe": **no constant-rate run from the released initialisation at the c… | — | **SUPPORTED** |
| 232 | 8. The released checkpoint | He replied the same day; the exchange is reproduced in full, anonymised, in the supplementary material (`SUPPLEMENTARY_CORRESPONDENCE.md`): the releas… | — | **SUPPORTED** |
| 233 | 8. The released checkpoint | It is that **the released artifacts do not reproduce the released checkpoint's variance state, and the author's account is that the released repositor… | — | **SUPPORTED** |
| 234 | 9. Method | An append-only ledger.** Every claim here has a permanent identifier, an evidence class (source, data, run, external, inference) and a status, in `FIN… | `FINDINGS_LEDGER.md` | **SUPPORTED** |
| 235 | 9. Method | Figure 4 gives each lead time from commit timestamps for all {{f4_n_rules}} rules; {{f4_n_positive}} are positive and {{f4_n_negative}} is not. | `paper_figures.json` | **SUPPORTED** |
| 236 | 9. Method | The negative one is the duplication-control rule (§7.4), which was stated in conversation before the runs but reached git **{{lead_task3}} after they … | `paper_figures.json` | **SUPPORTED** |
| 237 | 9. Method | {{n_retractions_word}} retractions on our own evidence**, out of {{n_superseded}} superseded claims kept in the record, plus {{n_retract_framing_word}… | `FINDINGS_LEDGER.md` | **SUPPORTED** |
| 238 | 9. Method | It was named by position here until the second pre-submission review entered three more framing retractions and moved it. | — | **UNREVIEWED** |
| 239 | 9. Method | Resampling trajectories instead widens intervals by a mean {{bu_mean_ratio}}× and changes {{bu_changes}} of {{bu_cells}} verdicts, in an h = 8 cell al… | `review_bootstrap_unit.json` | **SUPPORTED** |
| 240 | 9. Method | Reproducibility, and a build that checks its own prose.** `./reproduce.sh --quick --force` regenerates {{ver_files}} artifact files and {{ver_values}}… | `verify_reproduction.json` | **SUPPORTED** |
| 241 | 9. Method | The build therefore also verifies **{{cc_n}} comparative claims** across {{cc_kinds}} kinds, each pinning a fragment of the paper's own text *and* a r… | `comparative_claims.json` | **SUPPORTED** |
| 242 | 9. Method | Appendix D gives the argument, the kinds, the self-test, the {{cc_selfdefects_lower}} defects the self-test has found in the checker itself, and the t… | `comparative_claims.json` | **UNREVIEWED** |
| 243 | 10. Actionable lessons | {{n_lessons_word}} things a practitioner can apply without reading the rest of this paper. | `PAPER.template.md, Actionable lessons` | **SUPPORTED** |
| 244 | 10. Actionable lessons | And expect it to degrade with horizon.** At one forecast step it ranks whole rollouts almost perfectly — {{d2_epi_h1}} {{d2_epi_ci_h1}} across the {{a… | `a2_trajectory_level_control.json`, `task_d_nind20.json` | **UNREVIEWED** |
| 245 | 10. Actionable lessons | Over the full rollout it falls to {{d4_r}} {{d4_ci}}. | `task_d_nind20.json` | **SUPPORTED** |
| 246 | 10. Actionable lessons | It still beats the free alternative — the forecast step index — at every horizon we tested, on a paired test that excludes zero at every horizon where… | `task_d_nind20.json` | **SUPPORTED** |
| 247 | 10. Actionable lessons | That is a real signal, not a re-encoding of how far ahead you are looking — and not merely a report of which episode is hard: with both the forecast d… | `a2_trajectory_level_control.json` | **SUPPORTED** |
| 248 | 10. Actionable lessons | But it is too small to be an interval by a wide margin — at h = {{v2_deploy_h}}, the horizon the method itself rolls out over, {{d1n_epi_ratio_h100}}×… | `task_d3_ens5.json`, `task_d_nind20.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 249 | 10. Actionable lessons | If you need the interval, rescale per horizon, not globally.** One multiplier per forecast horizon, fitted on held-out data, brings coverage within {{… | `task_d3_perhorizon.json` | **SUPPORTED** |
| 250 | 10. Actionable lessons | The held-out cells are {{d3_nhoriz}} horizons × two fold directions on the same {{d3_nind_tot}} trajectories, not independent trials, so read the swee… | `task_d3_perhorizon.json` | **SUPPORTED** |
| 251 | 10. Actionable lessons | The fitted multipliers span {{d3_epi_cspread}}× across horizons, which is precisely why one number cannot serve. | `task_d3_perhorizon.json` | **SUPPORTED** |
| 252 | 10. Actionable lessons | Do not convert per-dimension sign counts into P-values.** State dimensions in a robot are physically coupled and share a forecast-depth trend, so an i… | `task_b_permutation.json` | **SUPPORTED** |
| 253 | 10. Actionable lessons | The held-out arena here contains {{nind_oos_400}} independent 400-step trajectories however many windows are drawn from it, and that number — not the … | `review_bootstrap_unit.json` | **SUPPORTED** |
| 254 | 10. Actionable lessons | Reporting an interval beside a trajectory count rather than an independent-trajectory count overstates precision, and resampling pooled seed × traject… | `review_bootstrap_unit.json` | **SUPPORTED** |
| 255 | 10. Actionable lessons | Check that the implemented loss is the described loss before reproducing any number from it.** The paper describes two loss terms; the implementation … | `step4_3_differential.json` | **SUPPORTED** |
| 256 | 10. Actionable lessons | The predicted variance has an optimum at zero under the implemented one, which is why the released checkpoint's σ is {{d1n_alea_ratio_h100}}× smaller … | `task_d_nind20.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 257 | 11. Broader impact | But a downstream user who reads the same quantity as a *calibrated interval* — a safety margin, a confidence bound, a gate on when to hand control to … | `task_d_nind20.json`, `v2_deployment_horizon.json`, `v3_metric_definitions.json` | **SUPPORTED** |
| 258 | 11. Broader impact | We think that makes the finding worth publishing rather than the reverse, and it is the reason §6 reports coverage rather than only correlation. | — | **SUPPORTED** |
| 259 | 12. Limitations | Effective sample size bounds every long-horizon claim.** The out-of-sample arena has {{m23_nind}} independent 400-step trajectories. | `task5_analysis.json` | **SUPPORTED** |
| 260 | 12. Limitations | We since trained {{e5_seeds}} Arm A arms at ensemble size 5 (§6.7). | `task_d3_ens5.json` | **SUPPORTED** |
| 261 | 12. Limitations | They reproduce the *direction* of §6.7's finding in {{e5_lead_cells}} of {{e5_total_cells}} seed-horizon cells and the *calibration* failure at {{e5_r… | `task_d3_ens5.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 262 | 12. Limitations | The binding constraint is the same one this section opens with: our arms have a genuine held-out arena of only {{e5_nind}} independent trajectories, a… | `task_d3_ens5.json` | **SUPPORTED** |
| 263 | 12. Limitations | We did not measure what the miscalibration costs.** We show that the penalty the follow-up applies is miscalibrated as a scale — {{d1n_epi_ratio_h100}… | `task_d_nind20.json`, `v2_deployment_horizon.json` | **SUPPORTED** |
| 264 | 12. Limitations | The per-dimension ordering tests are underpowered at every sample size we can reach.** Once the coupling between state dimensions is respected (§6.6),… | `task_b_permutation.json` | **SUPPORTED** |
| 265 | 12. Limitations | The larger arenas can reject and do not: over all ten episodes the smallest P in the family is {{perm_all_holm_min_p}} against a threshold of {{perm_a… | `task_b_permutation.json` | **SUPPORTED** |
| 266 | 12. Limitations | The aggregate scalar the method applies is separately and more strongly supported (§6.7), on the same trajectories, because it is one test rather than… | — | **SUPPORTED** |
| 267 | 12. Limitations | They also differ in **capacity**: the independent arm carries {{v1_cap_indep}} state-pathway parameters against the shared-trunk arm's {{v1_cap_shared… | `v1_ensemble_topology.json` | **UNREVIEWED** |
| 268 | 12. Limitations | §6.4's mechanism is a structural fact plus a hypothesis, and the two are separable.** That the five members share a trunk, a hidden state and {{v1_sha… | `v1_ensemble_topology.json` | **SUPPORTED** |
| 269 | 13. Conclusion | At h = {{v2_deploy_h}}, the horizon the method's own imagination rollouts run to, the aleatoric σ is {{d1n_alea_ratio_h100}}× smaller than its own err… | `task_d_nind20.json`, `v2_deployment_horizon.json` | **SUPPORTED** |
| 270 | 13. Conclusion | The epistemic term the method actually penalises with is better by a factor of {{d1n_epi_over_alea_h100}} and still {{d1n_epi_ratio_h100}}× [{{d1n_epi… | `task_d_nind20.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 271 | 13. Conclusion | The more useful finding is asymmetric, and it cuts both ways. | — | **SUPPORTED** |
| 272 | 13. Conclusion | The scale failure is established and large — but it is repairable: a per-horizon multiplier, fitted on one held-out episode and scored on another, res… | `task_d3_perhorizon.json` | **SUPPORTED** |
| 273 | 13. Conclusion | And the ranking use the follow-up claims does survive a real test: against the forecast step index, a free baseline neither original paper ran, ensemb… | `task_d_nind20.json` | **SUPPORTED** |
| 274 | 13. Conclusion | The control this rests on is the one that removes trajectory difficulty rather than forecast depth**: with both the rollout and the depth held constan… | `a2_trajectory_level_control.json` | **SUPPORTED** |
| 275 | 13. Conclusion | That is a smaller number than the {{a2_r_pooled}} pooled figure and it is the one that means what a practitioner needs it to mean — so it is not a re-… | `a2_trajectory_level_control.json` | **SUPPORTED** |
| 276 | 13. Conclusion | That is the one claim of either original work that this reproduction strengthens rather than qualifies. | — | **SUPPORTED** |
| 277 | 13. Conclusion | Three of the five σ estimates we measured order their own errors better than chance in direction — the epistemic term on every one of the {{perm_all_e… | `task_b_permutation.json`, `v2_deployment_horizon.json` | **SUPPORTED** |
| 278 | 13. Conclusion | The released checkpoint's *aleatoric* head does the opposite, ranking error inversely at h = {{v2_diag_h}} on every one of {{perm_all_relale_ndim_h368… | `task_b_permutation.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
| 279 | 13. Conclusion | We report that rather than the independent-trials P-values an earlier draft carried, which were wrong by up to a factor of about {{perm_worst_factor}}… | `task_b_permutation.json` | **SUPPORTED** |
| 280 | Data and code | And **the repository was archived by Software Heritage on 21 August 2026**, before submission, under a permanent identifier whose visit timestamp is n… | — | **SUPPORTED** |
| 281 | Data and code | It proves that the repository, with the whole pre-registration history in the form this paper cites, existed no later than that archival moment, as re… | — | **SUPPORTED** |
| 282 | References | Read at v1; now at {{v4_current}}, last revised {{v4_current_date}}. §5.1 and Eq. 4–5 keep their numbers there; every figure and appendix table has mo… | `original_paper_figures.json` | **UNREVIEWED** |
| 283 | References | The two names never co-occur: v1 uses {{v4_name_v1}} {{v4_name_n_v1}} times and no {{v4_name_v3}}, v3 the reverse. | `original_paper_figures.json` | **UNREVIEWED** |
| 284 | References | Each was checked against the paper itself: title, full author list and venue from the arXiv record, and for any sentence this paper attributes, the se… | `t1_bibliography_verified.json` | **SUPPORTED** |
| 285 | Appendix B — reproducing | Training all {{rt_runs}} runs takes **{{rt_hours}} hours** of recorded wall clock on two CPU cores: {{rt_hours_10k}} hours for the {{rt_runs_10k}} run… | `step5_*.json` | **UNREVIEWED** |
| 286 | Appendix B — reproducing | An earlier version of this appendix said 22 hours; that figure predated the {{rt_runs_10k}} ten-thousand-iteration runs added for the three-seed headl… | `step5_*.json` | **SUPPORTED** |
| 287 | Appendix D — verifying the | The {{n_retractions_word_lower}} numbered retractions, in order.** In order: a premise about forecast decay that turned out not to exist in the code; … | `FINDINGS_LEDGER.md` | **SUPPORTED** |
| 288 | Appendix D — verifying the | The {{n_retract_framing_word}} framing retractions**, withdrawn as stated claims rather than as numbers, and generated from the ledger rather than lis… | `FINDINGS_LEDGER.md` | **UNREVIEWED** |
| 289 | Appendix D — verifying the | `S-19` is different in kind and worse in one respect: §8 had already narrowed that claim in the paper, and the narrowing was never entered in the ledg… | — | **UNREVIEWED** |
| 290 | Appendix D — verifying the | A retraction that holds in one document and not in the repository is not a retraction, and the `retraction-consistency` check now reads all five reade… | — | **UNREVIEWED** |
| 291 | Appendix D — verifying the | an interval relation that is not the one asserted** — "the intervals do not overlap", where at h=128 they overlap across 0.604–0.643; - **an extremum … | — | **SUPPORTED** |
| 292 | Appendix D — verifying the | A sixth failure mode is not a relation at all, and it defeated the gate rather than evading it.** `build_paper.py` asserted that no `{{`-delimited pla… | — | **UNREVIEWED** |
| 293 | Appendix D — verifying the | The check kinds.** `scripts/check_comparative_claims.py` verifies {{cc_n}} claims across {{cc_kinds}} kinds: {{cc_kind_list}}. | `comparative_claims.json` | **UNREVIEWED** |
| 294 | Appendix D — verifying the | That list is generated from the checker's own registry rather than written here. | — | **UNREVIEWED** |
| 295 | Appendix D — verifying the | Each entry pins two things and requires both: a **fragment of the paper's own text**, so that rewording a sentence fails the check rather than silentl… | — | **SUPPORTED** |
| 296 | Appendix D — verifying the | The self-test.** Every assertion is run against a deliberately corrupted expectation on each build and must fail: the interval relation inverted, the … | `comparative_claims.json` | **SUPPORTED** |
| 297 | Appendix D — verifying the | An assertion that has quietly stopped being able to fail is worth less than no assertion, because it reads as coverage. | — | **SUPPORTED** |
| 298 | Appendix D — verifying the | {{cc_selfdefects_word}} defects the self-test has found in the checker itself**, rather than in the paper. | `comparative_claims.json` | **UNREVIEWED** |
| 299 | Appendix D — verifying the | Each surfaced because the checks were run rather than assumed, and the last two are the ones a reader should weigh, because both are failures of *cove… | `comparative_claims.json` | **UNREVIEWED** |
| 300 | Appendix D — verifying the | Corruptions now invert relative to each claim's own expectation, every registered kind carries at least one claim, and every claim is corrupted on eve… | `comparative_claims.json` | **UNREVIEWED** |
| 301 | Appendix D — verifying the | This list is generated from the checker rather than written here, so a fourth entry cannot be forgotten. | — | **UNREVIEWED** |
| 302 | Appendix D — verifying the | The CPU budget.* {{ver_timing}} timing fields and the {{ver_machine}} values of `results/step4_5_timing.json` — projected runtimes for configurations … | `verify_reproduction.json` | **SUPPORTED** |
| 303 | Appendix D — verifying the | It cannot reproduce bitwise on another machine, or on this one under different load, and it records that about itself: across its {{time_cfgs}} config… | `step4_5_timing.json` | **SUPPORTED** |
| 304 | Appendix D — verifying the | One wall-clock-bounded diagnostic.* {{ver_timebound}} stops after {{ver_tb_budget}} seconds rather than at its {{ver_tb_cap}}-iteration cap, so it rea… | `step4_4_overfit_ens1.json`, `verify_reproduction.json` | **SUPPORTED** |
| 305 | Appendix D — verifying the | Excluding by filename rather than by stopping rule would have dropped the reproducible one along with it, so the verifier decides from the artifact — … | — | **SUPPORTED** |
| 306 | Appendix D — verifying the | The verifier now drops any key whose recorded source is an excluded artifact ({{ver_hostkeys}} of them), which follows the provenance the file already… | `verify_reproduction.json` | **SUPPORTED** |
| 307 | Appendix D — verifying the | One further class, excluded by the same mechanism and worth naming because it sounds like an excuse.** {{ver_selfref}} keys in `paper_numbers.json` ar… | `verify_reproduction.json` | **SUPPORTED** |
| 308 | Appendix D — verifying the | There is no fixed point to converge to, and treating it as a reproducibility failure would make the reported figure oscillate rather than settle. | — | **SUPPORTED** |
| 309 | Appendix D — verifying the | They are dropped by provenance like the others and counted in the output rather than hidden — the same discipline §9's own {{ver_files}}-file figure r… | `verify_reproduction.json` | **SUPPORTED** |
| 310 | Appendix E — what testing  | §4's table marks {{orig_n_tested}} claims tested and the rest not. "Not tested" is an apology unless it comes with a price, so here is what each would… | `original_paper_figures.json` | **SUPPORTED** |
| 311 | Appendix F — every claim o | References to arXiv:2504.16680 follow **v1**, which is the version we read; it is now at {{v4_current}} ({{v4_current_date}}), where §5.1 and Eq. 4–5 … | `original_paper_figures.json`, `v2_deployment_horizon.json` | **UNREVIEWED** |
