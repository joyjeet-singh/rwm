# Claims-versus-evidence audit (C1)

One row per declarative claim in the paper. Claims are enumerated mechanically from
`PAPER.template.md` by `scripts/task_c1_claims_audit.py`; the verdict column is the
only judgement, and is keyed to the claim's text so that rewording a claim resets it
to UNREVIEWED rather than carrying a stale verdict forward.

**212 claims.** SUPPORTED: 212

| # | § | claim | backed by | verdict |
|---|---|---|---|---|
| 1 | Abstract | The base paper's central claim reproduces, and by a wide margin: over {{d1_seeds}} training seeds, autoregressive training reaches normalised error **… | `task5_analysis.json`, `task_c3_multiplicity.json`, `task_d1_threeseed.json` | **SUPPORTED** |
| 2 | Abstract | At the 368-step deployment horizon the aleatoric σ is **{{d1n_alea_ratio_h368}}× smaller than the realised error** on n_independent = {{d1n_nind}} tra… | `task_d_nind20.json` | **SUPPORTED** |
| 3 | Abstract | Running the correction — the authors' own unused `gaussian_nll` branch — fails differently rather than succeeding, at {{cal_nll_ratio}}× overconfidenc… | `task1_calibration.json` | **SUPPORTED** |
| 4 | Abstract | The epistemic term, the one the method actually consumes, is {{d1n_epi_over_alea_h368}}× better and still **{{d1n_epi_ratio_h368}}× overconfident at t… | `task_d_nind20.json` | **SUPPORTED** |
| 5 | Abstract | Ensemble disagreement beats a trivial baseline.** Against the forecast step index — a counter, free, requiring no ensemble — disagreement correlates {… | `task_d_nind20.json` | **SUPPORTED** |
| 6 | Abstract | A paired test on the difference between the two, resampling whole trajectories, excludes zero at {{d2p_n_separating}} of {{d2p_n_horizons}} horizons, … | `task_d2b_robustness.json`, `task_d_nind20.json` | **SUPPORTED** |
| 7 | Abstract | And the interval is repairable.** A per-horizon multiplier, fitted on one held-out episode and scored on the other, brings coverage within {{d3_tol}} … | `task_d3_perhorizon.json` | **SUPPORTED** |
| 8 | Abstract | The teacher-forced arm has the most input-dependent σ ({{cal_armB_over_faithA_cov}}× the autoregressive arm's) and ranks its own errors on {{cal_armB_… | `task1_calibration.json` | **SUPPORTED** |
| 9 | Abstract | That count is *not* the strong evidence an independent-trials test makes it look like: the 45 state dimensions are physically coupled and share a fore… | `task_b_permutation.json` | **SUPPORTED** |
| 10 | Abstract | We also report {{n_defects}} defects in the released pipeline, and evidence that the released checkpoint's variance state is not reachable from the re… | `PAPER.template.md section 6 subsections` | **SUPPORTED** |
| 11 | Abstract | The gap is not marginal: the bound arithmetic implies **{{implied_iters}}** iterations against the {{unreach_recalled_iters}} recalled, a factor of ab… | `E4_AUTHOR_CONTACT.md`, `step6_analysis.json` | **SUPPORTED** |
| 12 | Abstract | And we report {{n_retractions_lower}} retractions of our own numbered claims, plus {{n_retract_framing_word}} more that withdraw framings rather than … | `FINDINGS_LEDGER.md` | **SUPPORTED** |
| 13 | 1. Introduction | A world model that reports its own uncertainty is more useful than one that does not, and the uncertainty-aware Robotic World Model reports one. | — | **SUPPORTED** |
| 14 | 1. Introduction | But the same rebuild made a second question cheap to ask, because we had a from-scratch model, the released checkpoint, and a harness that could score… | — | **SUPPORTED** |
| 15 | 1. Introduction | We rebuilt rather than imported.** The forward pass, the loss and the training step are written from scratch and then checked against the reference: o… | `step4_3_differential.json` | **SUPPORTED** |
| 16 | 1. Introduction | We retract our own findings when they fail.** {{n_retractions_word}} numbered claims in this work are withdrawn on evidence this project produced, and… | `FINDINGS_LEDGER.md` | **SUPPORTED** |
| 17 | 1. Introduction | All of them are kept in the record rather than deleted (§8). | — | **SUPPORTED** |
| 18 | 2. Setup | Data.** The released dataset is {{rows}} rows of ANYmal D proprioceptive state and policy actions at 50 Hz. | `step0_regimes.json` | **SUPPORTED** |
| 19 | 2. Setup | The reference window builder therefore marks all {{win_naive}} windows valid, including {{win_cross}} that splice one episode's end onto the next one'… | `step0_regimes.json` | **SUPPORTED** |
| 20 | 2. Setup | The usable, episode-respecting count is {{win_usable}} — {{rows}} rows, less {{win_tail}} that cannot start a full window, less {{win_cross}} that cro… | `step0_regimes.json` | **SUPPORTED** |
| 21 | 2. Setup | The contamination rate is {{contam_pct}}%. | `step0_regimes.json` | **SUPPORTED** |
| 22 | 2. Setup | The paper describes two loss terms; the implementation has {{diff_terms}}. | `step4_3_differential.json` | **SUPPORTED** |
| 23 | 2. Setup | Two 400-step trajectories whose spans overlap are not independent evidence, and the out-of-sample arena contains only {{m23_nind}} mutually non-overla… | `task5_analysis.json` | **SUPPORTED** |
| 24 | 3. What the original paper | The third column is what the original reports, and it is the answer to a question a reader of any reproduction should ask: is this result consistent w… | — | **SUPPORTED** |
| 25 | 3. What the original paper | For all {{orig_n_tested}} of the claims we tested, the answer is that there is nothing to compare against.** Each is asserted qualitatively and shown … | `original_paper_figures.json` | **SUPPORTED** |
| 26 | 3. What the original paper | So our {{d1_ratio}}× is not a confirmation of a published figure and not a contradiction of one — it is the first figure attached to the claim. | `task_d1_threeseed.json` | **SUPPORTED** |
| 27 | 3. What the original paper | Where a magnitude is legible only from a plotted curve we say so rather than estimating it from the axis. | — | **SUPPORTED** |
| 28 | 4. The base paper's centra | Three conditions, all required: the out-of-sample gap at h = 368 excludes zero under a bootstrap over independent trajectories; the sign is consistent… | — | **SUPPORTED** |
| 29 | 4. The base paper's centra | Result.** Every condition {{m23_c1}}. | `task5_analysis.json` | **SUPPORTED** |
| 30 | 4. The base paper's centra | The sign test, which does not depend on n.* At h = 368 the per-episode gap favours autoregressive training on **{{c3_sign_pos}} of {{c3_sign_n}}** epi… | `task_c3_multiplicity.json` | **SUPPORTED** |
| 31 | 4. The base paper's centra | Its scope needs stating plainly, because the arena labels invite a stronger reading than it supports.** {{n_train_eps}} of the {{c3_sign_n}} episodes … | `step5_armA_seed0.json (split)`, `task_c3_multiplicity.json` | **SUPPORTED** |
| 32 | 4. The base paper's centra | The out-of-sample effect size below carries that burden, on {{nind_oos_400}} independent trajectories. | `review_bootstrap_unit.json` | **SUPPORTED** |
| 33 | 4. The base paper's centra | The in-sample arena, where the sample is larger.* The same comparison on the eight training episodes has {{nind_ins_400}} independent 400-step traject… | `review_bootstrap_unit.json` | **SUPPORTED** |
| 34 | 4. The base paper's centra | The out-of-sample effect size, over {{d1_seeds}} seeds.* Autoregressive training reaches **{{d1_A_mean}} ± {{d1_A_sd}}** against teacher forcing's **{… | `task_d1_threeseed.json` | **SUPPORTED** |
| 35 | 4. The base paper's centra | Seed spread is not symmetric between the arms and that is worth stating: Arm A ranges {{d1_A_lo}}–{{d1_A_hi}} across seeds ({{d1_A_relsd}}% relative),… | `task_d1_threeseed.json` | **SUPPORTED** |
| 36 | 4. The base paper's centra | Teacher forcing is more than twice as variable across seeds as autoregressive training at this horizon, so a single-seed comparison of these two arms … | — | **SUPPORTED** |
| 37 | 4. The base paper's centra | An earlier draft of this paper quoted the single-seed figures {{m23_A}} and {{m23_B}}; those came from the seed that happened to be favourable to Arm … | `task5_analysis.json`, `task_d1_threeseed.json` | **SUPPORTED** |
| 38 | 4. The base paper's centra | For a single seed the bootstrap over trajectories gives 95% interval [{{m23_ci_lo}}, {{m23_ci_hi}}] on n = {{m23_nind}} independent trajectories. | `task5_analysis.json` | **SUPPORTED** |
| 39 | 4. The base paper's centra | That interval should not be read as an ordinary one:** four trajectories admit {{c3_resamples}} distinct resamples, so any bootstrap tail is quantised… | `task_c3_multiplicity.json` | **SUPPORTED** |
| 40 | 4. The base paper's centra | Against a baseline, because neither number means anything without one.* The hold-last floor — predicting that nothing changes — scores **{{floor_h368}… | `task4_arenas.json` | **SUPPORTED** |
| 41 | 4. The base paper's centra | Autoregressive training beats it by **{{floor_over_A}}×**. | `task4_arenas.json + task5_analysis.json` | **SUPPORTED** |
| 42 | 4. The base paper's centra | Teacher forcing is {{B_over_floor}}× worse than assuming nothing changes at all**, which is the sharper statement of what exposure bias costs here: th… | `task4_arenas.json + task5_analysis.json` | **SUPPORTED** |
| 43 | 4. The base paper's centra | What does not hold, and we say so.** At h = 8 — the horizon the model is trained on — the same comparison out-of-sample gives a gap of {{m23_h8_gap}} … | `task5_analysis.json` | **SUPPORTED** |
| 44 | 4. The base paper's centra | Under the correct cluster bootstrap, the out-of-sample gap excludes zero in **{{ab_long_excl}} of {{ab_long_cells}}** long-horizon cells — both trajec… | `review_bootstrap_unit.json` | **SUPPORTED** |
| 45 | 4. The base paper's centra | Multiplicity.** Those {{ab_long_cells}} cells sit in a family of {{c3_family}} out-of-sample comparisons, so we state the correction rather than leavi… | `review_bootstrap_unit.json`, `task_c3_multiplicity.json` | **SUPPORTED** |
| 46 | 4. The base paper's centra | All {{c3_bonf_excl}} of {{c3_long}} still exclude zero at a Bonferroni level of 0.05/{{c3_family}}, and Holm–Bonferroni rejects **{{c3_holm_rejected}}… | `task_c3_multiplicity.json` | **SUPPORTED** |
| 47 | 4.1 The data budget, which | The base paper's headline is a sample-efficiency result: policies transfer to hardware from {{c2_ref}} state transitions of world-model pretraining ag… | `task_c2_data_budget.json` | **SUPPORTED** |
| 48 | 4.1 The data budget, which | Our arms consume {{c2_trans}} distinct state transitions.** That is the {{c2_rows}} rows of the eight training episodes less one per episode boundary … | `task_c2_data_budget.json` | **SUPPORTED** |
| 49 | 4.1 The data budget, which | It is deliberately not the {{c2_windows}} training windows, which overlap almost completely — consecutive 33-step windows start one row apart — nor th… | `task_c2_data_budget.json` | **SUPPORTED** |
| 50 | 4.1 The data budget, which | Against the reference's {{c2_ref}}, that is **{{c2_ratio}}× less data, {{c2_pct}}% of its world-model budget**. | `task_c2_data_budget.json` | **SUPPORTED** |
| 51 | 4.1 The data budget, which | A dynamics model trained on {{c2_pct}}% of the reference's data still reproduces the autoregressive-versus-teacher-forcing result at {{d1_ratio}}× and… | `task4_arenas.json + task5_analysis.json`, `task_c2_data_budget.json`, `task_d1_threeseed.json` | **SUPPORTED** |
| 52 | 4.1 The data budget, which | Three limits, in the same breath.** It is not a reproduction of the {{c2_ref}}-against-250M comparison, which is about policy learning and which we do… | `task_c2_data_budget.json` | **SUPPORTED** |
| 53 | 4.1 The data budget, which | And our model is evaluated on the same narrow distribution it trained on — one robot, one gait, one terrain, velocity commands from a single bounded b… | `task_c2_data_budget.json` | **SUPPORTED** |
| 54 | 5.1 Which quantity the met | We asked, and the first author confirms the code is operative: the penalty is applied to the standard deviation as intended, and Eq. 4 is "more of a h… | — | **SUPPORTED** |
| 55 | 5.1 Which quantity the met | It is reported in Fig. 3 (right) as an analysis of the model behavior." So what follows is not an implementation slip being reported back to its autho… | — | **SUPPORTED** |
| 56 | 5.1 Which quantity the met | It is three things the papers do not address: that the aleatoric head is discarded before use, that neither quantity is usable as a scale, and that th… | — | **SUPPORTED** |
| 57 | 5.2 The measurement | All four rows are the held-out arena** — the two episodes withheld from our own arms, n_independent = {{b2_nind}} — because that is the only arena on … | `task_b2_epistemic.json` | **SUPPORTED** |
| 58 | 5.2 The measurement | On the aleatoric head every model is overconfident by between one and four orders of magnitude (Figure 1); that is the quantity §5.1 shows the method … | — | **SUPPORTED** |
| 59 | 5.2 The measurement | A note on the released checkpoint's row, so the next table does not read as a contradiction.* Its {{cal_rel_ratio}}× is measured on those same {{b2_ni… | `task1_calibration.json`, `task_b2_epistemic.json` | **SUPPORTED** |
| 60 | 5.2 The measurement | The released checkpoint trained on all ten episodes, so its own best-sampled figure is the {{d1n_alea_ratio_h368}}× below, at n_independent = {{d1n_ni… | `task_d_nind20.json` | **SUPPORTED** |
| 61 | 5.2 The measurement | The quantity the method does use is also uncalibrated.** On the released {{b2_members}}-member checkpoint over all {{d1n_eps}} episodes, n_independent… | `task_b2_epistemic.json`, `task_d_nind20.json` | **SUPPORTED** |
| 62 | 5.2 The measurement | We use all ten rather than the held-out pair here because the released checkpoint trained on all ten, so restricting it to two buys no independence an… | — | **SUPPORTED** |
| 63 | 5.2 The measurement | The held-out-only version at n_independent = {{b2_nind}} is in the supplementary material (`results/task_b2_epistemic.json`). | `task_b2_epistemic.json` | **SUPPORTED** |
| 64 | 5.2 The measurement | The epistemic column agrees in direction with this one at all {{agree_epi}} of {{agree_nh}} horizons; the aleatoric column agrees at {{agree_alea}} of… | `task_b2_epistemic.json + task_d_nind20.json` | **SUPPORTED** |
| 65 | 5.2 The measurement | Epistemic is {{d1n_epi_over_alea_h368}}× better than aleatoric at the deployment horizon and still wrong by **{{d1n_epi_ratio_h1}}×** at one step and … | `task_d_nind20.json` | **SUPPORTED** |
| 66 | 5.2 The measurement | The larger sample changes one thing materially, and it is a correction to our own earlier reading.** At n_independent = {{b2_nind}} the epistemic orde… | `task_b2_epistemic.json` | **SUPPORTED** |
| 67 | 5.2 The measurement | At n_independent = {{d1n_nind}} it is {{d1n_epi_npos_h1}} of {{d1n_epi_ndim_h1}} at h=1, with mean r = {{d1n_epi_r_h1}}, the *strongest* mean correlat… | `task_d_nind20.json` | **SUPPORTED** |
| 68 | 5.2 The measurement | The short-horizon "chance" result was an artifact of four trajectories, not a property of the model, and we record it as such rather than keeping the … | — | **SUPPORTED** |
| 69 | 5.2 The measurement | The released checkpoint is no longer the only ensemble measured.** Three Arm A arms at ensemble size 5 (§5.6, {{e5_seeds}} seeds, out-of-sample, n_ind… | `task_d3_ens5.json` | **SUPPORTED** |
| 70 | 5.2 The measurement | Our arms are **better calibrated than the released checkpoint and fail the same way**: {{e5_ratio_h368}}× overconfident at the deployment horizon agai… | `task_d3_ens5.json`, `task_d_nind20.json` | **SUPPORTED** |
| 71 | 5.2 The measurement | The last column of the table above gives permutation P-values over whole trajectories, not binomial ones, computed on the same {{perm_all_nind}} traje… | `task_b_permutation.json` | **SUPPORTED** |
| 72 | 5.2 The measurement | These are five tests on one family and none survives Holm–Bonferroni across the arena's {{perm_all_holm_n}} cells — the smallest is {{perm_all_holm_mi… | `task_b_permutation.json` | **SUPPORTED** |
| 73 | 5.2 The measurement | The scalar penalty as actually applied — `means.std(0).sum(-1)` at `envs/base.py:166` — correlates **{{d4_r}}** with total absolute error over the rol… | `task_d_nind20.json` | **SUPPORTED** |
| 74 | 5.3 Why the aleatoric head | Across all {{n_runs}} runs the collapse is linear in iteration count and its rate is nearly identical (Figure 3a). | `step5_arm*.json` | **SUPPORTED** |
| 75 | 5.3 Why the aleatoric head | Rates are fitted on {{e2_fitted_runs}} of those runs: the {{e2_excluded_10k}} 10,000-iteration runs are excluded from the rate statistics because they… | `step5_arm*.json` | **SUPPORTED** |
| 76 | 5.3 Why the aleatoric head | Figure 3(a) shows all {{run_total}} runs; Figure 3(b) plots only the {{e2_fitted_runs}} the rate is fitted on, so the scatter and the quoted statistic… | `step5_arm*.json` | **SUPPORTED** |
| 77 | 5.3 Why the aleatoric head | The {{run_total}} runs, so a reader can count them: | `step5_arm*.json` | **SUPPORTED** |
| 78 | 5.3 Why the aleatoric head | Two different things are being explained here, and §5.5 separates them.** *Magnitude collapse is objective-driven.* It occurs in all {{e2_mse_runs}} s… | `step5_arm*.json` | **SUPPORTED** |
| 79 | 5.3 Why the aleatoric head | Input-independence is not.* That varies by a factor of {{cal_armB_over_faithA_cov}} between two arms trained under the same objective, so the objectiv… | `task1_calibration.json` | **SUPPORTED** |
| 80 | 5.4 The correction fails d | Running it reverses the collapse and improves the magnitude from {{cal_faithA_ratio}}× to {{cal_nll_ratio}}× overconfident. | `task1_calibration.json` | **SUPPORTED** |
| 81 | 5.4 The correction fails d | It does not produce a usable estimate, and it destroys something the faithful arm had: the σ-versus-error ordering falls from {{cal_faithA_npos}}/{{ca… | `task1_calibration.json` | **SUPPORTED** |
| 82 | 5.4 The correction fails d | Under the trajectory permutation test of §5.5 those counts give P = {{perm_oos_faithA_p_h368}} and {{perm_oos_nll_p_h368}} out of sample, {{perm_ins_f… | `task_b_permutation.json` | **SUPPORTED** |
| 83 | 5.5 The failure is one of  | Our ensemble-5 arms have both: their aleatoric CoV is comparable to the other arms', and their *epistemic* term is far more input-dependent than any a… | `task1_calibration.json`, `task_d3_ens5.json` | **SUPPORTED** |
| 84 | 5.5 The failure is one of  | The count column is the out-of-sample arena** (n_independent = {{relale_oos_nind}}), so that all four models are compared on trajectories none of our … | `task_b_permutation.json` | **SUPPORTED** |
| 85 | 5.5 The failure is one of  | It is not the only arena, and for the released checkpoint's aleatoric head it is not the most informative one: at n_independent = {{relale_all_nind}} … | `task_b_permutation.json` | **SUPPORTED** |
| 86 | 5.5 The failure is one of  | Arm B's σ is {{cal_armB_over_faithA_cov}}× more input-dependent than the faithful arm's, and it has the largest mean correlation of the four (r = {{ca… | `task1_calibration.json` | **SUPPORTED** |
| 87 | 5.5 The failure is one of  | It is still {{cal_armB_ratio}}× overconfident. | `task1_calibration.json` | **SUPPORTED** |
| 88 | 5.5 The failure is one of  | More importantly, error grows with rollout depth in every trajectory, so *any* σ that also grows with depth correlates with *any* trajectory's error —… | — | **SUPPORTED** |
| 89 | 5.5 The failure is one of  | The worst-affected cell is {{perm_worst_model}} at h={{perm_worst_h}}, in the {{perm_worst_arena}} arena. | `task_b_permutation.json` | **SUPPORTED** |
| 90 | 5.5 The failure is one of  | It moves from {{perm_ins_armB_binom_h368}} to {{perm_ins_armB_p_h368}} — a factor of about {{perm_worst_factor}} — because under a null that preserves… | `task_b_permutation.json` | **SUPPORTED** |
| 91 | 5.5 The failure is one of  | Observing {{perm_ins_armB_npos_h368}} of {{perm_ins_armB_ndim_h368}} against that null is close to unremarkable. | `task_b_permutation.json` | **SUPPORTED** |
| 92 | 5.5 The failure is one of  | A fair coin, by contrast, centres the count at 22.5 of 45; the dependence-preserving null centres it between {{perm_null_lo}} and {{perm_null_hi}} dep… | `task_b_permutation.json` | **SUPPORTED** |
| 93 | 5.5 The failure is one of  | The teacher-forced arm collapses in magnitude exactly like the autoregressive ones — same objective, same rate — while retaining {{cal_armB_over_faith… | `task1_calibration.json` | **SUPPORTED** |
| 94 | 5.5 The failure is one of  | The same pattern holds for the quantity the method uses, and this is where the correction bites hardest.** At h=128 and h=368 the epistemic term corre… | `task_b2_epistemic.json` | **SUPPORTED** |
| 95 | 5.5 The failure is one of  | All figures in this paragraph are the held-out arena (n_independent = {{b2_nind}})**, so that the epistemic term and the four aleatoric heads are comp… | `task_b2_epistemic.json`, `task_d_nind20.json` | **SUPPORTED** |
| 96 | 5.5 The failure is one of  | It does not beat Arm B's head on strength either: its mean correlation at h=368 is {{b2_epi_r_h368}} against {{cal_armB_r}}. | `task1_calibration.json`, `task_b2_epistemic.json` | **SUPPORTED** |
| 97 | 5.5 The failure is one of  | Under the permutation null that count gives P = {{perm_oos_epi_p_h368}} out of sample and {{perm_ins_epi_p_h368}} in sample, against {{perm_oos_epi_bi… | `task_b_permutation.json` | **SUPPORTED** |
| 98 | 5.5 The failure is one of  | It still fails the horizon test the same way: σ grows {{b2_epi_sigma_growth}}× from h=1 to h=368 while error grows {{b2_epi_err_growth}}×. | `task_b2_epistemic.json` | **SUPPORTED** |
| 99 | 5.5 The failure is one of  | The horizon story we first told was backwards, and the larger arenas agree with each other against the smallest.** At n_independent = {{perm_oos_nind}… | `task_b_permutation.json` | **SUPPORTED** |
| 100 | 5.5 The failure is one of  | In sample (n_independent = {{perm_ins_nind}}): {{perm_ins_epi_p_h1}} at h=1, {{perm_ins_epi_p_h8}} at h=8, against {{perm_ins_epi_p_h128}} at h=128. | `task_b_permutation.json` | **SUPPORTED** |
| 101 | 5.5 The failure is one of  | Over all ten episodes (n_independent = {{perm_all_nind}}): {{perm_all_epi_p_h1}}, {{perm_all_epi_p_h8}} and {{perm_all_epi_p_h128}}. | `task_b_permutation.json` | **SUPPORTED** |
| 102 | 5.5 The failure is one of  | At long horizon the shared forecast-depth trend lifts the null to {{perm_all_epi_null_h128}} of 45, so a count of 45 is close to what chance alone del… | `task_b_permutation.json` | **SUPPORTED** |
| 103 | 5.5 The failure is one of  | The out-of-sample arena is not wrong so much as blind: at {{perm_oos_nind}} trajectories its smallest attainable P-value is {{perm_oos_floor}}, so it … | `task_b_permutation.json` | **SUPPORTED** |
| 104 | 5.5 The failure is one of  | We report the small arena's numbers alongside because it is the only arena that is out-of-sample for our own arms, not because it is the better measur… | — | **SUPPORTED** |
| 105 | 5.5 The failure is one of  | Nothing here survives multiplicity correction, in any of the three arenas.** Holm–Bonferroni over each arena's {{perm_oos_holm_n}} model × horizon cel… | `task_b_permutation.json` | **SUPPORTED** |
| 106 | 5.5 The failure is one of  | Out of sample that is a property of the design rather than of the models: with {{perm_oos_nind}} independent trajectories the smallest attainable P-va… | `task_b_permutation.json` | **SUPPORTED** |
| 107 | 5.5 The failure is one of  | In sample the miss is real — the smallest P in the family is {{perm_ins_holm_min_cell}} at {{perm_ins_holm_min_p}} against a threshold of {{perm_ins_h… | `task_b_permutation.json` | **SUPPORTED** |
| 108 | 5.5 The failure is one of  | So the honest form of this section's claim is narrower than the one we first wrote. | — | **SUPPORTED** |
| 109 | 5.6 Ensemble disagreement  | Section 5.5 shows the per-dimension version of that claim is weaker than it looks. | — | **SUPPORTED** |
| 110 | 5.6 Ensemble disagreement  | This section asks a different and, for a practitioner, more important question: **does disagreement beat something free? | — | **SUPPORTED** |
| 111 | 5.6 Ensemble disagreement  | All three correlations below are on the scalar quantity the method actually applies — `means.std(0).sum(-1)` at `envs/base.py:166` — against total abs… | `task_d_nind20.json` | **SUPPORTED** |
| 112 | 5.6 Ensemble disagreement  | Disagreement wins at every horizon tested.** The counter reaches {{d2b_idx_h368}} over the full rollout against disagreement's {{d2b_epi_h368}}, and t… | `task_d_nind20.json` | **SUPPORTED** |
| 113 | 5.6 Ensemble disagreement  | The paired difference — resampling whole trajectories and recomputing *both* correlations inside each draw — is the appropriate test and the more powe… | — | **SUPPORTED** |
| 114 | 5.6 Ensemble disagreement  | It excludes zero at **{{d2p_n_separating}} of {{d2p_n_horizons}}** horizons. | `task_d_nind20.json` | **SUPPORTED** |
| 115 | 5.6 Ensemble disagreement  | At {{d2p_overlap_h}} the marginal intervals *do* overlap — it is the horizon where the counter is strongest ({{d2b_idx_h128}}) and the margin narrowes… | `task_d_nind20.json` | **SUPPORTED** |
| 116 | 5.6 Ensemble disagreement  | The paired difference there is {{d2p_diff_h128}} {{d2p_ci_h128}}, which excludes zero, but only just: {{d2p_narrowest_lo}} is the smallest lower bound… | `task_d_nind20.json` | **SUPPORTED** |
| 117 | 5.6 Ensemble disagreement  | The third column answers a different question: is disagreement merely re-encoding the clock?** Partialling the step index out of both variables *lower… | `task_d_nind20.json` | **SUPPORTED** |
| 118 | 5.6 Ensemble disagreement  | Partialling out log(1 + index) gives {{d2r_log}} {{d2r_log_ci}}; a cubic in the index gives {{d2r_cub}} {{d2r_cub_ci}}; a rank partial correlation, wh… | `task_d2b_robustness.json` | **SUPPORTED** |
| 119 | 5.6 Ensemble disagreement  | Computing the correlation **within each forecast step** — across trajectories, with depth held exactly constant, so the index cannot contribute by con… | `task_d2b_robustness.json` | **SUPPORTED** |
| 120 | 5.6 Ensemble disagreement  | The weakest figure across all {{d2r_ncontrols}} controls is {{d2r_weakest}}. | `task_d2b_robustness.json` | **SUPPORTED** |
| 121 | 5.6 Ensemble disagreement  | It returns {{e5_verdict}}.** The first condition passes completely — disagreement leads the index in **{{e5_lead_cells}} of {{e5_total_cells}}** seed-… | `task_d3_ens5.json` | **SUPPORTED** |
| 122 | 5.6 Ensemble disagreement  | The second fails: the paired difference excludes zero at {{e5_n_excl}} of {{e5_n_horizons}} horizons, not a majority. | `task_d3_ens5.json` | **SUPPORTED** |
| 123 | 5.6 Ensemble disagreement  | What separates the two conditions is sample size, and we measured that rather than asserting it.** Our own arms can only be scored out-of-sample on th… | `task_d3_ens5.json`, `task_d_nind20.json` | **SUPPORTED** |
| 124 | 5.6 Ensemble disagreement  | Subsampling four trajectories at a time from a twenty-trajectory pool, the rule's criterion fires on {{e5_power_mean}}% of draws on average and on onl… | `task_d3b_ens5_power.json` | **SUPPORTED** |
| 125 | 5.6 Ensemble disagreement  | That estimate is an **upper bound**, because the pool it subsamples is in-sample for these arms, where the effect is {{e5_eff_ins}} against {{e5_eff_o… | `task_d3b_ens5_power.json` | **SUPPORTED** |
| 126 | 5.6 Ensemble disagreement  | Reported as a companion and not as a discharge:* on all ten episodes (n_independent = {{e5_comp_nind}}, **in-sample** for these arms, which trained on… | `task_d3b_ens5_power.json` | **SUPPORTED** |
| 127 | 5.6 Ensemble disagreement  | It cannot discharge M-43, which is stated over the out-of-sample arena, and we record it only so the comparison with the released checkpoint's {{d1n_n… | `task_d_nind20.json` | **SUPPORTED** |
| 128 | 5.6 Ensemble disagreement  | We ran the baseline test expecting it to go the other way.** A counter matching disagreement would have been the more consequential result — it would … | — | **SUPPORTED** |
| 129 | 5.7 One constant scalar do | On the epistemic term — the quantity the method uses — a scalar of {{d2_epi_c_lo}}–{{d2_epi_c_hi}} brings h=1 coverage to {{d2_epi_cov1_lo}}–{{d2_epi_… | `task_d2_recalibration.json` | **SUPPORTED** |
| 130 | 5.7 One constant scalar do | On the aleatoric term a scalar of {{d2_ale_c_lo}}–{{d2_ale_c_hi}} gives {{d2_ale_cov1_lo}}–{{d2_ale_cov1_hi}}% at h=1 and {{d2_ale_cov368_lo}}–{{d2_al… | `task_d2_recalibration.json` | **SUPPORTED** |
| 131 | 5.7 One constant scalar do | The two held-out episodes contribute {{d3_nind_tot}} non-overlapping 400-step trajectories between them, so each direction fits on n_independent = {{d… | `task_d3_perhorizon.json` | **SUPPORTED** |
| 132 | 5.7 One constant scalar do | Every held-out cell lands within {{d3_tol}} points of the {{d3_target}}% target for both quantities. | `task_d3_perhorizon.json` | **SUPPORTED** |
| 133 | 5.7 One constant scalar do | The largest deviation over all {{d3_ncells_all}} held-out cells is {{d3_worst_q}} at h={{d3_worst_h}}, fitted on episode {{d3_worst_ep}} and scored on… | `task_d3_perhorizon.json` | **SUPPORTED** |
| 134 | 5.7 One constant scalar do | The two largest deviations are both at h={{d3_worst_h}} on the aleatoric term, in opposite directions ({{d3_worst_cov}}% and {{d3_second_cov}}%), whic… | `task_d3_perhorizon.json` | **SUPPORTED** |
| 135 | 5.7 One constant scalar do | The constant scalar manages {{d3_epi_const_ok}} of {{d3_epi_cells}}, and those are the h=1 cells it was fitted at. | `task_d3_perhorizon.json` | **SUPPORTED** |
| 136 | 5.7 One constant scalar do | The per-horizon scalar has five free parameters against the constant one's one, so it *must* fit better in sample — only the held-out column above is … | — | **SUPPORTED** |
| 137 | 5.7 One constant scalar do | And the correction is a calibration patch, not a fix: it leaves the model's σ carrying no more information than before and simply rescales it by how f… | — | **SUPPORTED** |
| 138 | 5.8 The structural excuse  | The faithful arm's σ *declines* ({{sig_faithA_growth}}×) while its error grows {{err_faithA_growth}}×. | `task2_sigma_profile.json` | **SUPPORTED** |
| 139 | 6. Defects in the released | The window builder reads a termination column that is identically zero, so it marks all {{win_naive}} windows valid. | `step0_regimes.json` | **SUPPORTED** |
| 140 | 6. Defects in the released | Scored correctly the released checkpoint is materially better than its own released evaluation reports: nRMSE at h = 368 falls from {{stale_nrmse}} un… | `step4_0a_results.json` | **SUPPORTED** |
| 141 | 6. Defects in the released | 6.4 What the spliced windows cost: nothing measurable.** We trained a contaminated arm on {{arm_contam_windows}} windows — the clean {{arm_clean_windo… | `step5_armA_seed0.json`, `step5_armA_seed0_contam.json` | **SUPPORTED** |
| 142 | 6. Defects in the released | The arm's contamination rate is {{arm_contam_pct}}%, against the reference pipeline's {{contam_pct}}%. | `step0_regimes.json`, `step5_armA_seed0_contam.json` | **SUPPORTED** |
| 143 | 6. Defects in the released | It is deliberately lower: we splice only the {{bound_both_train}} boundaries whose *both* sides are training episodes, because {{bound_touch_holdout}}… | `step5_armA_seed0.json (split)` | **SUPPORTED** |
| 144 | 6. Defects in the released | That is a leakage problem rather than a physics one, and including it would have invalidated our own comparison. | — | **SUPPORTED** |
| 145 | 6. Defects in the released | Training loss over the final 250 iterations: duplication costs {{dup_cost_pct}}%, splicing costs {{contam_cost_pct}}%. | `task3_control_arm.json` | **SUPPORTED** |
| 146 | 6. Defects in the released | The bootstrap interval on duplicated − clean is [{{dup_ci_lo}}, {{dup_ci_hi}}], including zero. | `task3_control_arm.json` | **SUPPORTED** |
| 147 | 6. Defects in the released | In rollout, across {{tw_cells}} cells (two arenas × two trajectory lengths × two checkpoints × two horizons × two metrics), contamination hurts in **{… | `task3_three_way.json` | **SUPPORTED** |
| 148 | 6. Defects in the released | The control is inert, differing from clean in {{tw_dc_cluster_helped}} cells. | `task3_three_way.json` | **SUPPORTED** |
| 149 | 6. Defects in the released | "Costs nothing" is the wrong summary, and we should not use it.** Splicing raises training loss by {{contam_cost_pct}}% against duplication's {{dup_co… | `task3_control_arm.json`, `task3_three_way.json` | **SUPPORTED** |
| 150 | 6. Defects in the released | Both are measured effects in opposite directions, which is the signature of regularisation: the spliced windows contain transitions the model cannot f… | — | **SUPPORTED** |
| 151 | 7. The released checkpoint | Fitting it across our runs and extrapolating to the released checkpoint's σ state implies **{{implied_iters}}** optimisation steps at the configured l… | `step6_analysis.json` | **SUPPORTED** |
| 152 | 7. The released checkpoint | The refit from our 10,000-iteration runs gives {{q4_implied_A}} and {{q4_implied_B}}, spreading {{implied_spread_pct}}% across the three fits — a line… | `task5_analysis.json`, `step6_analysis.json` | **SUPPORTED** |
| 153 | 7. The released checkpoint | So the defensible claim is narrower than "cannot have come from the released recipe": **no constant-rate run from the released initialisation at the c… | — | **SUPPORTED** |
| 154 | 7. The released checkpoint | He replied the same day: the released `max_iterations: 500` is "a typo"; his recollection is 5,000 iterations, "as I always did"; he does not recall h… | — | **SUPPORTED** |
| 155 | 7. The released checkpoint | It is that **the released artifacts do not reproduce the released checkpoint's variance state, and the author's account is that the released repositor… | — | **SUPPORTED** |
| 156 | 8. Method | An append-only ledger.** Every claim here has a permanent identifier, an evidence class (source, data, run, external, inference) and a status, in `FIN… | `FINDINGS_LEDGER.md` | **SUPPORTED** |
| 157 | 8. Method | The fifth bar is negative: the duplication-control rule (§6.4) was stated in conversation before the runs but reached git **{{lead_task3}} after they … | `paper_figures.json` | **SUPPORTED** |
| 158 | 8. Method | {{n_retractions_word}} retractions on our own evidence**, out of {{n_superseded}} superseded claims kept in the record, plus {{n_retract_framing_word}… | `FINDINGS_LEDGER.md` | **SUPPORTED** |
| 159 | 8. Method | Resampling trajectories instead widens intervals by a mean {{bu_mean_ratio}}× and changes {{bu_changes}} of {{bu_cells}} verdicts, in an h = 8 cell al… | `review_bootstrap_unit.json` | **SUPPORTED** |
| 160 | 8. Method | Reproducibility, and a build that checks its own prose.** `./reproduce.sh --quick --force` regenerates {{ver_files}} artifact files and {{ver_values}}… | `verify_reproduction.json` | **SUPPORTED** |
| 161 | 8. Method | The build therefore also verifies **{{cc_n}} comparative claims** across {{cc_kinds}} kinds, each pinning a fragment of the paper's own text *and* a r… | `comparative_claims.json` | **SUPPORTED** |
| 162 | 9. Actionable lessons | {{n_lessons_word}} things a practitioner can apply without reading the rest of this paper. | `PAPER.template.md section 9` | **SUPPORTED** |
| 163 | 9. Actionable lessons | And expect it to degrade with horizon.** At one forecast step it correlates {{d2_epi_h1}} {{d2_epi_ci_h1}} with realised error — very nearly a perfect… | `task_d_nind20.json` | **SUPPORTED** |
| 164 | 9. Actionable lessons | Over the full {{d1n_nind}}-trajectory rollout it falls to {{d4_r}} {{d4_ci}}. | `task_d_nind20.json` | **SUPPORTED** |
| 165 | 9. Actionable lessons | It still beats the free alternative — the forecast step index — at every horizon we tested, on a paired test that excludes zero at {{d2p_n_separating}… | `task_d_nind20.json` | **SUPPORTED** |
| 166 | 9. Actionable lessons | That is a real signal, not a re-encoding of how far ahead you are looking: holding forecast depth exactly constant it still correlates {{d2r_win}} wit… | `task_d2b_robustness.json` | **SUPPORTED** |
| 167 | 9. Actionable lessons | But it is too small to be an interval by a wide margin — {{d1n_epi_ratio_h368}}× on the released checkpoint and {{e5_ratio_h368}}× on the ensemble-5 a… | `task_d3_ens5.json`, `task_d_nind20.json` | **SUPPORTED** |
| 168 | 9. Actionable lessons | If you need the interval, rescale per horizon, not globally.** One multiplier per forecast horizon, fitted on held-out data, brings coverage within {{… | `task_d3_perhorizon.json` | **SUPPORTED** |
| 169 | 9. Actionable lessons | The fitted multipliers span {{d3_epi_cspread}}× across horizons, which is precisely why one number cannot serve. | `task_d3_perhorizon.json` | **SUPPORTED** |
| 170 | 9. Actionable lessons | Do not convert per-dimension sign counts into P-values.** State dimensions in a robot are physically coupled and share a forecast-depth trend, so an i… | `task_b_permutation.json` | **SUPPORTED** |
| 171 | 9. Actionable lessons | The held-out arena here contains {{nind_oos_400}} independent 400-step trajectories however many windows are drawn from it, and that number — not the … | `review_bootstrap_unit.json` | **SUPPORTED** |
| 172 | 9. Actionable lessons | Reporting an interval beside a trajectory count rather than an independent-trajectory count overstates precision, and resampling pooled seed × traject… | `review_bootstrap_unit.json` | **SUPPORTED** |
| 173 | 9. Actionable lessons | Check that the implemented loss is the described loss before reproducing any number from it.** The paper describes two loss terms; the implementation … | `step4_3_differential.json` | **SUPPORTED** |
| 174 | 9. Actionable lessons | The predicted variance has an optimum at zero under the implemented one, which is why the released checkpoint's σ is {{d1n_alea_ratio_h368}}× smaller … | `task_d_nind20.json` | **SUPPORTED** |
| 175 | 10. Broader impact | But a downstream user who reads the same quantity as a *calibrated interval* — a safety margin, a confidence bound, a gate on when to hand control to … | `task_d_nind20.json` | **SUPPORTED** |
| 176 | 10. Broader impact | We think that makes the finding worth publishing rather than the reverse, and it is the reason §5 reports coverage rather than only correlation. | — | **SUPPORTED** |
| 177 | 11. Limitations | Effective sample size bounds every long-horizon claim.** The out-of-sample arena has {{m23_nind}} independent 400-step trajectories. | `task5_analysis.json` | **SUPPORTED** |
| 178 | 11. Limitations | We since trained {{e5_seeds}} Arm A arms at ensemble size 5 (§5.6). | `task_d3_ens5.json` | **SUPPORTED** |
| 179 | 11. Limitations | They reproduce the *direction* of §5.6's finding in {{e5_lead_cells}} of {{e5_total_cells}} seed-horizon cells and the *calibration* failure at {{e5_r… | `task_d3_ens5.json` | **SUPPORTED** |
| 180 | 11. Limitations | The binding constraint is the same one this section opens with: our arms have a genuine held-out arena of only {{e5_nind}} independent trajectories, a… | `task_d3_ens5.json` | **SUPPORTED** |
| 181 | 11. Limitations | We did not measure what the miscalibration costs.** We show that the penalty the follow-up applies is miscalibrated as a scale — {{d1n_epi_ratio_h368}… | `task_d_nind20.json` | **SUPPORTED** |
| 182 | 11. Limitations | The per-dimension ordering tests are underpowered at every sample size we can reach.** Once the coupling between state dimensions is respected (§5.5),… | `task_b_permutation.json` | **SUPPORTED** |
| 183 | 11. Limitations | The larger arenas can reject and do not: over all ten episodes the smallest P in the family is {{perm_all_holm_min_p}} against a threshold of {{perm_a… | `task_b_permutation.json` | **SUPPORTED** |
| 184 | 11. Limitations | The aggregate scalar the method applies is separately and more strongly supported (§5.6), on the same trajectories, because it is one test rather than… | — | **SUPPORTED** |
| 185 | 12. Conclusion | The aleatoric σ is {{d1n_alea_ratio_h368}}× smaller than its own error, and the cause is that the objective's optimum is σ = 0 with the term that shou… | `task_d_nind20.json` | **SUPPORTED** |
| 186 | 12. Conclusion | The epistemic term the method actually penalises with is {{d1n_epi_over_alea_h368}}× better and still {{d1n_epi_ratio_h368}}× overconfident where it i… | `task_d_nind20.json` | **SUPPORTED** |
| 187 | 12. Conclusion | The more useful finding is asymmetric, and it cuts both ways. | — | **SUPPORTED** |
| 188 | 12. Conclusion | The scale failure is established and large — but it is repairable: a per-horizon multiplier, fitted on one held-out episode and scored on another, res… | `task_d3_perhorizon.json` | **SUPPORTED** |
| 189 | 12. Conclusion | And the ranking use the follow-up claims does survive a real test: against the forecast step index, a free baseline neither original paper ran, ensemb… | `task_d2b_robustness.json`, `task_d_nind20.json` | **SUPPORTED** |
| 190 | 12. Conclusion | That is the one claim of either original work that this reproduction strengthens rather than qualifies. | — | **SUPPORTED** |
| 191 | 12. Conclusion | Three of the five σ estimates we measured order their own errors better than chance in direction — the epistemic term on {{perm_all_epi_npos_h368}} of… | `task_b_permutation.json` | **SUPPORTED** |
| 192 | 12. Conclusion | The released checkpoint's *aleatoric* head does the opposite, ranking error inversely on every one of {{perm_all_relale_ndim_h368}} dimensions over al… | `task_b_permutation.json` | **SUPPORTED** |
| 193 | 12. Conclusion | We report that rather than the independent-trials P-values an earlier draft carried, which were wrong by up to a factor of about {{perm_worst_factor}}… | `task_b_permutation.json` | **SUPPORTED** |
| 194 | Data and code | And **the repository was archived by Software Heritage on 21 August 2026**, before submission, under a permanent identifier whose visit timestamp is n… | — | **SUPPORTED** |
| 195 | Data and code | It proves that the repository, with the whole pre-registration history in the form this paper cites, existed no later than that archival moment, as re… | — | **SUPPORTED** |
| 196 | Appendix B — reproducing | Training all {{rt_runs}} runs takes **{{rt_hours}} hours** of recorded wall clock on two CPU cores: {{rt_hours_10k}} hours for the {{rt_runs_10k}} run… | `step5_*.json` | **SUPPORTED** |
| 197 | Appendix B — reproducing | The longest single run is {{rt_longest}} hours. | `step5_*.json` | **SUPPORTED** |
| 198 | Appendix B — reproducing | An earlier version of this appendix said 22 hours; that figure predated the {{rt_runs_10k}} ten-thousand-iteration runs added for the three-seed headl… | `step5_*.json` | **SUPPORTED** |
| 199 | Appendix D — verifying the | The {{n_retractions_word_lower}} numbered retractions, in order.** In order: a premise about forecast decay that turned out not to exist in the code; … | `FINDINGS_LEDGER.md` | **SUPPORTED** |
| 200 | Appendix D — verifying the | The {{n_retract_framing_word}} framing retractions are the claim that a pre-registration was pre-registered, and the binomial inference of §5.5. | `FINDINGS_LEDGER.md` | **SUPPORTED** |
| 201 | Appendix D — verifying the | an interval relation that is not the one asserted** — "the intervals do not overlap", where at h=128 they overlap across 0.604–0.643; - **an extremum … | — | **SUPPORTED** |
| 202 | Appendix D — verifying the | The check kinds.** `scripts/check_comparative_claims.py` verifies {{cc_n}} claims across {{cc_kinds}} kinds: *overlap* (two intervals do or do not ove… | `comparative_claims.json` | **SUPPORTED** |
| 203 | Appendix D — verifying the | Each entry pins two things and requires both: a **fragment of the paper's own text**, so that rewording a sentence fails the check rather than silentl… | — | **SUPPORTED** |
| 204 | Appendix D — verifying the | The self-test.** Every assertion is run against a deliberately corrupted expectation on each build and must fail: the interval relation inverted, the … | `comparative_claims.json` | **SUPPORTED** |
| 205 | Appendix D — verifying the | An assertion that has quietly stopped being able to fail is worth less than no assertion, because it reads as coverage. | — | **SUPPORTED** |
| 206 | Appendix D — verifying the | Both were defects in the checker rather than in the paper, and both surfaced because the checks were run rather than assumed. | — | **SUPPORTED** |
| 207 | Appendix D — verifying the | The CPU budget.* {{ver_timing}} timing fields and the {{ver_machine}} values of `results/step4_5_timing.json` — projected runtimes for configurations … | `verify_reproduction.json` | **SUPPORTED** |
| 208 | Appendix D — verifying the | It cannot reproduce bitwise on another machine, or on this one under different load, and it records that about itself: across its {{time_cfgs}} config… | `step4_5_timing.json` | **SUPPORTED** |
| 209 | Appendix D — verifying the | One wall-clock-bounded diagnostic.* {{ver_timebound}} stops after {{ver_tb_budget}} seconds rather than at its {{ver_tb_cap}}-iteration cap, so it rea… | `step4_4_overfit_ens1.json`, `verify_reproduction.json` | **SUPPORTED** |
| 210 | Appendix D — verifying the | Excluding by filename rather than by stopping rule would have dropped the reproducible one along with it, so the verifier decides from the artifact — … | — | **SUPPORTED** |
| 211 | Appendix D — verifying the | The verifier now drops any key whose recorded source is an excluded artifact ({{ver_hostkeys}} of them), which follows the provenance the file already… | `verify_reproduction.json` | **SUPPORTED** |
| 212 | Appendix E — what testing  | §3's table marks {{orig_n_tested}} claims tested and the rest not. "Not tested" is an apology unless it comes with a price, so here is what each would… | `original_paper_figures.json` | **SUPPORTED** |
